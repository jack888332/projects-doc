# 第024篇

> 本篇定位：财务结算-订单成本管理；主要内容：成本来源、审批、结算明细、调整单与查询；文档角色：模块档；文档ID：70AD0AF66B

**关联业务主流程：** [税金管理与财务结算流程](mainflow/PRD.高捷物流系统一期.主流程.08.税金管理与财务结算流程.md#mainflow-08)。
**关联模块流程：** [结算管理流程](PRD.高捷物流系统一期.第033篇.DDP-报价运输与结算流程.7579B595B8.md#doc-7579B595B8-section-a5b92ee23329)。
<a id="doc-70AD0AF66B-section-73aeb84b1dce"></a>
## 1. 财务结算-订单成本管理


**业务入口：** 订单成本页面。

**概念模型：** [数据字典 · 财务结算实体](PRD.高捷物流系统一期.第035篇.数据字典.7A3D9E1F50.md#doc-7A3D9E1F50-domain-finance)

### 1.1 结算业务范围、角色与流程

<a id="doc-70AD0AF66B-section-e5d5791a938a"></a>
#### 1.1.1 业务目标与范围

订单成本管理覆盖应收、应付结算明细的新增、调整、提交、审批和查询。

<a id="doc-70AD0AF66B-section-64054fd81271"></a>
#### 1.1.2 财务结算操作入口

| 内容 | 一级菜单 | 二级菜单 | 说明 |
| --- | --- | --- | --- |
| 财务结算 | 基本信息管理 | 即期汇率维护 | 维护业务的即期汇率，当月的汇率全部从该地方获取，包括：即期汇率列表、搜索条件和新增即期汇率 |
|  |  | 成本项目维护 | 维护业务的成本项目，包括成本项目列表、搜索条件和新增成本项目。 |
|  |  | 开票单位维护 | 维护客户的开票信息，包括：客户开票单位列表、搜索条件、新增、调整、启用、停用。 |
|  |  | 银行账号管理 | 维护高捷的银行账户，包括：银行账户列表、搜索条件、新增、调整、启用、停用。 |
|  |  | 授信额度审批 | 审批提交的授信额度 |
|  |  | 合作方档案审批 | 审批新增 |
|  | 结算成本管理 | 订单成本 |  |
|  |  | 调整单 |  |
|  | 结算对账管理 | 应付对账单 |  |
|  |  | 应收对账单 |  |
|  | 收付款申请管理 | 付款申请 |  |
|  |  | 付款发票补录 |  |
|  |  | 收款申请 |  |
|  | 销项发票管理 | 开票申请 |  |
|  |  | 手工发票补录 |  |
|  |  | 销项票 |  |
|  | 实际收付款核销 | 付款核销 |  |
|  |  | 收款核销 |  |

<a id="doc-70AD0AF66B-section-85b92922d02d"></a>
#### 1.1.3 参与角色与职责

| 用户角色 | 描述 |
| --- | --- |
| 客服 | 在系统上录入或调整结算费用，结算费用提交审批；<br>创建调整单，调整单提交审批；<br>创建应收应付对账单、应收应付对账单确认；<br>创建付款申请，付款申请提交审批；<br>补录付款发票。 |
| 客服主管 | 在系统上审批结算费用；<br>审批调整单；<br>审批付款申请。 |
| 财务会计 | 审批结算费用；<br>审批调整单；<br>审批调付款申请；<br>创建收款申请；<br>确认收款申请；<br>创建销项票申请；<br>手工补录销项票；<br>审核合作方新增档案；<br>审核客户授信额度。 |
| 财务出纳 | 创建、修改、提交付款核销；<br>创建、修改、提交收款核销。 |
| 信息部 | 打回付款核销记录；<br>打回收款核销记录。 |

<a id="doc-70AD0AF66B-section-120050118a71"></a>
#### 1.1.4 应收结算流程

| 步骤 | 步骤描述 | 角色 | 流程描述 | 下一步骤 | 关键控制点 |
| --- | --- | --- | --- | --- | --- |
| 1 | 录入结算费用 | 业务员 | 业务服务完成或异常结束后，系统自动生成或业务员手工录入对应订单的应收费用 | 2 | 是 |
| 2 | 提交审批 | 业务员 | 应收应付费用都维护完成后，提交业务主管审批 | 3 |  |
| 3 | 审核结算费用 | 业务主管 | 业务主管审批结算费用(可整单审批，也可按行审批)。 | 4 |  |
| 4 | 审核是否通过 | 业务主管 | 费用审批通过则进行步骤5操作；费用审批拒绝则进行步骤7操作 | 5/7 |  |
| 5-1 | 单票账单打印 | 业务员 | 业务主管审批通过的结算费用，可打印出单票账单 | - |  |
| 5-2 | 审核结算费用 | 财务 | 财务审批结算费用(可整单审批，也可按行审批) | 6 |  |
| 6 | 审核是否通过 | 财务 | 费用审批通过则进行步骤8操作；费用审批拒绝则进行步骤7操作 | 7/8 |  |
| 7 | 修改还是作废结算费用明细 | 业务员 | 业务员确认修改还是作废结算明细,作废则进行步骤9操作；修改则进行步骤10操作 | 9/10 |  |
| 8 | 自动生成结算明细 | 系统 | 财务对结算费用明细审批通过后，系统自动生成已最终确认的结算明细，并将结算明细同步金蝶系统，应收结算明细生成应收单 | 11 | 是 |
| 9 | 作废该结算明细 | 业务员 | 业务员对审批拒绝的结算明细进行作废删除 | - |  |
| 10 | 修改并重新提交结算明细 | 业务员 | 业务员修改结算明细并重新提交 | 2 |  |
| 11 | 生成对账单 | 业务员/财务 | 业务员或财务，按照订单号、客户、结算单位、毛件体、目的地、费用项目、订单创建人、订单创建日期、费用录入人、费用录入日期等条件，生成对账单（1个对账单中结算单位必须相同） | 12 | 是 |
| 12 | 客户确认对账单 | 业务员/财务 | 对账单给到客户确认 | 13 |  |
| 13 | 对账单/结算是否有误 | 业务员/财务 | 根据客户反馈情况或自身核查情况，确认对账单是否有异常，有异常则删除对账单，进行步骤14操作创建调整单进行调整则；无异常可收款则确认对账单，并进行步骤15操作 | 14/15 |  |
| 14 | 录入调整单 | 业务员/财务 | 根据客户反馈情况或自身核查情况确认有误，基于财务审核通过的结算明细录入调整单，并提交审批。 | 2 | 是 |
| 15 | 收款申请创建 | 财务 | 财务基于对账完成后的结算明细且收款水单发起收款申请，并提交财务确认 | 16 | 是 |
| 16 | 收款申请确认 | 财务 | 财务对收款申请进行确认 | 17 |  |
| 17 | 收款申请审批是否通过 | 财务 | 审批通过，则进行步骤18操作；审批拒绝则进行则返回步骤15 | 18 |  |
| 18-1 | 销项票登记 | 财务 | 进行销项票登记 | - | 是 |
| 18-2 | 出纳收款核销 | 财务 | 出纳线下收款，线上进行收款核销 | 19 | 是 |
| 19 | 释放客户额度 | 财务 | 出纳核销完收款后，释放客户的额度 |  |  |

<a id="doc-70AD0AF66B-section-92839d0217c8"></a>
#### 1.1.5 应付结算流程

| 步骤 | 步骤描述 | 角色 | 流程描述 | 下一步骤 | 关键控制点 |
| --- | --- | --- | --- | --- | --- |
| 1 | 录入结算费用 | 业务员 | 业务服务完成或异常结束后，系统自动生成或业务员手工录入对应订单的应付费用，内部单位的应付费用由应收方维护（应付方可查看) | 2 | 是 |
| 2 | 提交审批 | 业务员 | 应收应付费用都维护完成后，提交业务主管审批 | 3 |  |
| 3 | 审核结算费用 | 业务主管 | 业务主管审批结算费用(可整单审批，也可按行审批)。 | 4 |  |
| 4 | 审核是否通过 | 业务主管 | 费用审批通过则进行步骤5操作；费用审批拒绝则进行步骤7操作 | 5/7 |  |
| 5-1 | 单票账单打印 | 业务员 | 业务主管审批通过的结算费用，可打印出单票账单 | - |  |
| 5-2 | 审核结算费用 | 财务 | 财务审批结算费用(可整单审批，也可按行审批) | 6 |  |
| 6 | 审核是否通过 | 财务 | 费用审批通过则进行步骤8操作；费用审批拒绝则进行步骤7操作 | 7/8 |  |
| 7 | 修改还是作废结算费用明细 | 业务员 | 业务员确认修改还是作废结算明细,作废则进行步骤9操作；修改则进行步骤10操作 | 9/10 |  |
| 8 | 自动生成结算明细 | 系统 | 财务对结算费用明细审批通过后，系统自动生成已最终确认的结算明细，并将结算明细同步金蝶系统，应付结算明细生成应付单 | 11 | 是 |
| 9 | 作废该结算明细 | 业务员 | 业务员对审批拒绝的结算明细进行作废删除 | - |  |
| 10 | 修改并重新提交结算明细 | 业务员 | 业务员修改结算明细并重新提交 | 2 |  |
| 11 | 生成对账单 | 业务员/财务 | 业务员或财务，按照订单号、客户、结算单位、毛件体、目的地、费用项目、订单创建人、订单创建日期、费用录入人、费用录入日期等条件，生成对账单（1个对账单中结算单位必须相同） | 12 | 是 |
| 12 | 供应商确认对账单 | 业务员/财务 | 对账单给到供应商确认 | 13 |  |
| 13 | 对账单/结算是否有误 | 业务员/财务 | 根据供应商反馈情况或自身核查情况，确认对账单是否有异常，有异常则删除对账单，进行步骤14操作创建调整单进行调整则；无异常可付款则确认对账单，并进行步骤15操作 | 14/15 |  |
| 14 | 录入调整单 | 业务员/财务 | 根据供应商反馈情况或自身核查情况确认有误，基于财务审核通过的结算明细录入调整单，并提交审批。 | 2 | 是 |
| 15 | 付款申请创建 | 业务员 | 业务员基于对账完成后的结算明细发起付款申请，并提交职能部门审批 | 16 | 是 |
| 16 | 付款申请审批 | 职能部门 | 职能部门（业务主管、分管领导、财务）对付款申请进行审批 | 17 |  |
| 17 | 付款申请审批是否通过 | 职能部门 | 审批通过，则进行步骤18操作；审批拒绝则返回步骤15 | 18 |  |
| 18 | 出纳付款核销 | 财务 | 出纳线下付款，线上进行付款核销 |  | 是 |

<a id="doc-70AD0AF66B-section-f3a6b8c29d10"></a>
#### 1.1.6 订单成本金额与汇率规则

本规则适用于应收、应付成本的新增和编辑，以及包含对应字段的调整单明细：

- 应收成本行的单价参考值（人民币）＝应付栏相同成本项目行的人民币金额合计÷数量合计。该值仅在页面展示，不保存；点击后将其带入含税单价（原币）。
- 总金额（原币）＝含税单价（原币）×数量，系统自动计算；调整单在保存时同步更新。
- 即期汇率按币种和成本行创建日期从即期汇率表取得。约定汇率默认等于即期汇率，允许修改；新增时修改币种，系统同步更新即期汇率和约定汇率。
- 约定汇率超即期汇率（%）＝（约定汇率－即期汇率）÷即期汇率×100。
- 约定汇率超即期汇率（%）大于 3 时，该值高亮；提交审批时提示“行号 + （子）单号，约定汇率高于即期汇率 XX%”。
- 人民币金额＝总金额（原币）×约定汇率。
- 应收合计（CNY）为订单全部“应收”结算明细行的人民币金额之和；应付合计（CNY）为订单全部“应付”结算明细行的人民币金额之和；利润合计（CNY）＝应收合计（CNY）－应付合计（CNY）。

<a id="doc-70AD0AF66B-section-f84d83fbfe27"></a>
### 1.2 结算成本管理功能与规则

<a id="doc-70AD0AF66B-section-5d608b45c471"></a>
#### 1.2.1 订单列表页面

订单列表页面，可搜索所有“已完成”的订单信息，并基于订单录入应收、应付成本。

（1）订单列表界面

（2）按钮说明

- 查询：在不选择任何查询字段下，点击“查询”按钮，默认所有查询条件。选择对应的查询条件，则显示符合结果的数据。查询出的数据，按照“业务日期”顺序排列。（业务日期为各订单服务完成的最终日期）

- 截止录入日期：按订单业务日期加 3 个工作日计算并展示。

- 重置：重置所有查询条件。

- 成本编辑：跳转“订单成本维护”界面。

（3）查询栏字段说明

**订单列表查询栏字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写订单号，支持模糊查询； |
| 提单号 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写提单号，支持模糊查询； |
| 客户 | 取值形式：文本 | 非必填；无默认值 | 手工录入；<br>填写客户名称，支持模糊查询； |
| 业务类型 | 取值形式：文本 | 非必填；无默认值 | LOV选择，支持模糊匹配；<br>不选择则默认全部，支持模糊查询； |
| 创建人 | 取值形式：文本 | 非必填；无默认值 | 手工录入；<br>填写创建人，支持模糊查询 |
| 业务日期 | 取值形式：日期 | 非必填；无默认值 | 日期选择框；<br>不选择默认全部日期 |

（4）订单列表栏字段

每页显示50条

**订单列表栏字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：60 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件则默认全部 |
| 业务类型 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件则默认全部 |
| 客户 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件则默认全部 |
| 创建人 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件则默认全部 |
| 业务日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件则默认全部 |
| 截止录入日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 业务含义与计算规则见[订单成本列表规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-5d608b45c471)。 |
| 提单号 | 取值形式：文本；长度或格式：60 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件则默认全部 |
| 审批备注 | 取值形式：文本 | 非必填；无默认值；不可修改 | 对应订单录入的审批意见 |

<a id="doc-70AD0AF66B-section-63a88707c661"></a>
#### 1.2.2 订单成本维护列表

（1）订单成本维护列表界面

订单列表展示需要录入成本的订单。点击“成本编辑”按钮，系统进入“订单成本维护列表”页面。

（2）按钮说明

- 提交：勾选对应数据，提交给主管、财务审核，弹出“提交确认”弹框。“已提交”、“业务审批通过”、“财务审批通过”状态下的行数据，无法再次提交。

- 返回：返回“订单列表”界面。

- 附件上传：上传该应收成本凭证。

- 编辑：弹出“订单成本编辑”弹框。“新建”、“业务审批拒绝”、“财务审批拒绝”3种状态的数据才有编辑功能，可对该行成本进行编辑修改。

- 删除：弹出“删除确认”弹框。“新建”、“业务审批拒绝”、“财务审批拒绝”3种状态的数据才有删除功能，可对该行成本进行删除操作。

- 保存：手工新增应收应付时，输入完成后，需进行“保存”操作。

<a id="doc-70AD0AF66B-section-316f3f944eaf"></a>
#### 1.2.3 应收成本新增页面

（1）订单成本新增-应收界面

订单成本维护列表界面，点击“新增应收”按钮，在最下方新增出一行。支持下拉框录入多行应收。

（2）按钮说明

- 保存：保存该行数据，校验必填字段是否填写，若必填字段未填写，无法保存。

- 删除：删除本行新增数据。

（3）应收成本行说明

**应收成本行字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 序号 | 取值形式：数字 | 非必填；无默认值；不可修改 | 系统自动生成记录序号。 |
| (子)单号 | 取值形式：LOV；长度或格式：20 | 必填；无默认值；可修改 | 1. LOV选择；<br>1. 只能选择该订单以及该订单关联的子单。 |
| 状态 | 取值形式：字符 | 非必填；有默认值；不可修改 | 1、系统默认；<br>2、新增时状态默认“新建”。 |
| 结算单位 | 取值形式：LOV | 必填；无默认值；可修改 | 1. LOV选择；<br>1. 也可手工录入关键字，支持模糊匹配。 |
| 成本项目 | 取值形式：LOV | 必填；无默认值；可修改 | 1. LOV选择；<br>1. 也可手工录入关键字，支持模糊匹配；<br>3、特殊说明：根据订单头的“业务类型”，在“业务类型与成本项关系”中查询与该“业务类型”关联的所有“成本项目”。若该类型下无该成本项目，需要联系IT或财务补充维护。 |
| 成本属性 | 取值形式：文本 | 非必填；无默认值；不可修改 | 1. 系统自动生成；<br>1. 点击“新增应收”，新增的成本属性默认为“应收”；<br>1. 当费用项目为“押金”时，需要对修改成本属性为“代收”。 |
| 数量 | 取值形式：数字 | 必填；无默认值；可修改 | 1. 手工录入；<br>1. 数量需为数字格式。 |
| 计费单位 | 取值形式：LOV | 必填；无默认值；可修改 | 手工录入计费单位 |
| 币种 | 取值形式：LOV；长度或格式：3 | 必填；有默认值；可修改 | 1. LOV选择；<br>1. LOV展示2列（币种代码、币种名称）；<br>1. 默认CNY。 |
| 税率(%) | 取值形式：LOV | 必填；有默认值；可修改 | 1、根据合作方档案，在客户表中查询对应结算单位的税率；<br>2、允许修改，LOV选择。 |
| 含税单价(原币) | 取值形式：数字 | 必填；无默认值；可修改 | 1. 手工录入；<br>1. 含税单价(原币)需为数字格式。 |
| 单价参考值<br>(人民币) | 取值形式：数字 | 必填；无默认值；不可修改 | 系统自动生成；展示、取值与带入规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 总金额(原币) | 取值形式：数字 | 非必填；无默认值；不可修改 | 系统自动生成；计算规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 即期汇率 | 取值形式：数字 | 非必填；有默认值；不可修改 | 系统自动生成；取值与联动规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 约定汇率 | 取值形式：数字 | 必填；有默认值；可修改 | 默认值与联动规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 约定汇率超<br>即期汇率(%) | 取值形式：数字 | 非必填；无默认值；不可修改 | 系统自动生成；计算、展示与提交提示见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 人民币金额 | 取值形式：数字 | 非必填；无默认值；不可修改 | 系统自动生成；计算规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 更新人 | 取值形式：文本 | 非必填；有默认值；不可修改 | 1. 系统自动生成；<br>1. 进行该新增操作对应的用户。 |
| 更新日期 | 取值形式：日期 | 非必填；有默认值；不可修改 | 1、系统自动生成；<br>2、进行该新增操作对应系统日期。 |
| 备注 | 取值形式：文本；长度或格式：200 | 非必填；无默认值；可修改 | 1. 手工录入； |
| 附件 | 取值形式：超链接 | 非必填；无默认值；可修改 | 1. 手工上传附件，支持文本、图片格式；<br>1. 大小≤20M。 |
| 审批备注 | 取值形式：文本 | 非必填；无默认值；不可修改 | 1. 审批后根据审批意见自动带出；<br>1. 默认只展示最后一次审批的审批备注。 |

<a id="doc-70AD0AF66B-section-0e2ea094e531"></a>
#### 1.2.4 应付成本新增页面

（1）订单成本新增-应付界面

订单成本维护列表界面，点击“新增应付”按钮，在最下方新增出一行。支持下拉框录入多行应付。

（2）按钮说明

- 保存：保存该行数据，校验必填字段是否填写，若必填字段未填写，无法保存。

- 删除：删除本行新增数据。

（3）应付成本行说明

**应付成本行字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 序号 | 取值形式：数字 | 非必填；无默认值；不可修改 | 系统自动生成记录序号。 |
| (子)单号 | 取值形式：LOV；长度或格式：20 | 必填；无默认值；可修改 | 1. LOV选择；<br>1. 只能选择该订单以及该订单关联的子单 |
| 状态 | 取值形式：字符 | 非必填；有默认值；不可修改 | 1、系统默认；<br>2、新增时状态默认“新建” |
| 结算单位 | 取值形式：LOV | 必填；无默认值；可修改 | 1. LOV选择；<br>1. 也可手工录入关键字，支持模糊匹配。 |
| 成本项目 | 取值形式：LOV | 必填；无默认值；可修改 | 1. LOV选择；<br>1. 也可手工录入关键字，支持模糊匹配；<br>3、特殊说明：根据订单头的“业务类型”，在“业务类型与成本项关系”中查询与该“业务类型”关联的所有“成本项目”。若该类型下无该成本项目，需要联系IT或财务补充维护。 |
| 成本属性 | 取值形式：文本 | 非必填；无默认值；不可修改 | 1. 系统自动生成；<br>1. 点击“新增应付”，新增的成本属性默认为“应付”；<br>1. 当费用项目为“押金”时，需要对修改成本属性为“代付”。 |
| 数量 | 取值形式：数字 | 必填；无默认值；可修改 | 1、手工录入；<br>2、数量需为数字格式。 |
| 计费单位 | 取值形式：LOV | 必填；无默认值；可修改 | 手工录入计费单位 |
| 币种 | 取值形式：LOV；长度或格式：3 | 必填；有默认值；可修改 | 1、LOV选择；<br>2、LOV展示2列（币种代码、币种名称）；<br>3、默认CNY。 |
| 税率(%) | 取值形式：LOV | 必填；有默认值；可修改 | 1、根据合作方档案，在供应商表中查询对应结算单位的税率；<br>2、允许修改，LOV选择。 |
| 含税单价(原币) | 取值形式：数字 | 必填；无默认值；可修改 | 1. 手工录入；<br>1. 含税单价(原币)需为数字格式。 |
| 总金额(原币) | 取值形式：数字 | 非必填；无默认值；不可修改 | 系统自动生成；计算规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 即期汇率 | 取值形式：数字 | 非必填；有默认值；不可修改 | 系统自动生成；取值与联动规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 约定汇率 | 取值形式：数字 | 必填；有默认值；可修改 | 默认值与联动规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 约定汇率超<br>即期汇率(%) | 取值形式：数字 | 非必填；无默认值；不可修改 | 系统自动生成；计算、展示与提交提示见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 人民币金额 | 取值形式：数字 | 非必填；无默认值；不可修改 | 系统自动生成；计算规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 更新人 | 取值形式：文本 | 非必填；有默认值；不可修改 | 1. 系统自动生成；<br>1. 进行该新增操作对应的用户。 |
| 更新日期 | 取值形式：日期 | 非必填；有默认值；不可修改 | 1、系统自动生成；<br>2、进行该新增操作对应系统日期。 |
| 备注 | 取值形式：文本；长度或格式：200 | 非必填；无默认值；可修改 | 手工录入 |
| 审批备注 | 取值形式：文本 | 非必填；无默认值；不可修改 | 1. 审批后根据审批意见自动带出；<br>1. 默认只展示最后一次审批的审批备注。 |
| 附件 | 取值形式：超链接 | 非必填；无默认值；可修改 | 1. 手工上传附件，支持文本、图片格式；<br>1. 大小≤20M。 |

<a id="doc-70AD0AF66B-section-c5e17177865b"></a>
#### 1.2.5 订单成本编辑页面

（1）应收编辑界面

订单成本维护列表界面，对应行点击“编辑”按钮，弹出该“订单成本编辑”弹框。只有“新建”状态保存后、“业务审批拒绝”、“财务审批拒绝”状态的订单成本行，才有编辑功能。

（2）按钮说明

- 确认：完成该行数据，校验必填字段是否填写，若必填字段未填写，无法保存。保存完成后，根据该编辑操作人员，自动更新该行的更新人和更新日期，其中更新日期为保存时的系统日期。

- 取消：取消本次编辑操作。

（3）订单成本编辑字段说明

**订单成本编辑字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 成本属性 | 取值形式：文本 | 非必填；有默认值；不可修改 | 1. 该编辑行自动带出；<br>1. 若本次编辑的为应收栏的成本项目；成本项目修改为“税金”，则成本属性在保存时需修改为“代收”；若本次编辑的成本项目修改为非“税金”，则成本属性修改为“应收”。<br>1. 若本次编辑的为应付栏的成本项目，成本项目修改为“税金”，则成本属性在保存时需修改为“代付”；若本次编辑的成本项目修改为非“税金”，则成本属性修改为“应付”。 |
| (子)单号 | 取值形式：LOV；长度或格式：20 | 必填；有默认值；可修改 | 1、该编辑行自动带出；<br>2、LOV选择；<br>1. 只能选择该订单以及该订单关联的子单。 |
| 结算单位 | 取值形式：LOV | 必填；有默认值；可修改 | 1. 该编辑行自动带出；<br>1. LOV选择；<br>1. 也可手工录入关键字，支持模糊匹配。 |
| 成本项目 | 取值形式：LOV | 必填；有默认值；可修改 | 1. 该编辑行自动带出；<br>1. LOV选择；<br>1. 也可手工录入关键字，支持模糊匹配；<br>4、特殊说明：根据订单头的“业务类型”，在“业务类型与成本项关系”中查询与该“业务类型”关联的所有“成本项目”。若该类型下无该成本项目，需要联系IT或财务补充维护。 |
| 数量 | 取值形式：数字 | 必填；有默认值；可修改 | 1、该编辑行自动带出；<br>2、手工录入；<br>1. 数量需为数字格式。 |
| 计费单位 | 取值形式：LOV | 必填；有默认值；可修改 | 1、该编辑行自动带出；<br>2、手工录入计费单位。 |
| 币种 | 取值形式：LOV；长度或格式：3 | 必填；有默认值；可修改 | 1、该编辑行自动带出；<br>2、LOV选择；<br>3、LOV展示2列（币种代码、币种名称）。 |
| 税率(%) | 取值形式：LOV | 必填；有默认值；可修改 | 1. 该编辑行自动带出；<br>1. LOV选择。 |
| 含税单价(原币) | 取值形式：数字 | 必填；有默认值；可修改 | 1. 该编辑行自动带出；<br>1. 手工录入；<br>1. 含税单价(原币)需为数字格式。 |
| 总金额(原币) | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据编辑行自动带出并由系统生成；计算规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 即期汇率 | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据编辑行自动带出并由系统生成；取值与联动规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 约定汇率 | 取值形式：数字 | 必填；有默认值；可修改 | 根据编辑行自动带出；默认值与联动规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 约定汇率超<br>即期汇率(%) | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据编辑行自动带出并由系统生成；计算、展示与提交提示见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 人民币金额 | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据编辑行自动带出并由系统生成；计算规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 备注 | 取值形式：文本；长度或格式：200 | 非必填；有默认值；可修改 | 1. 该编辑行自动带出；<br>1. 手工录入。 |

<a id="doc-70AD0AF66B-section-2216fea5c02f"></a>
#### 1.2.6 订单成本删除操作

（1）应收删除操作界面

订单成本维护界面，对应行点击“删除”按钮，弹出“删除确认”弹框

只有“新建”、“业务审批拒绝”、“财务审批拒绝”状态的订单成本行，才有删除功能。

（2）按钮说明

- 保存：完成对该行数据的删除，返回订单成本维护-应收界面。该行数据在“订单成本维护”删除。

- 取消：取消本次删除操作。

<a id="doc-70AD0AF66B-section-8bba8406a0e7"></a>
#### 1.2.7 订单成本提交操作

（1）订单成本提交界面

订单成本维护界面，勾选对应的订单行数据，点击“提交”按钮，弹出“提交确认”弹框。

（2）按钮说明

- 确认：确认勾选对应数据，提交给主管、财务审核，只有“新建”、“业务审批拒绝”、“财务审批拒绝”的可提交审批，“已提交”、“业务审批通过”、“财务审批通过”状态下的数据，无法再次提交。确认后，返回对应页签下的订单成本维护界面（应收页签/应付页签）。

- 返回：取消提交，返回对应页签下的订单成本维护界面（应收页签/应付页签）。

<a id="doc-70AD0AF66B-section-eb3a5ce5ecc9"></a>
#### 1.2.8 订单成本审批列表

订单成本审批列表页面，显示所有订单结算行状态为“已提交”、“业务审批通过”的订单数据和调整单数据。对于同个订单，有调整单的，需优先展示调整单，且操作审批时，进入页面为调整单的审批页面。

（1）订单审批列表界面

（2）按钮说明

- 查询：在不选择任何查询字段下，点击“查询”按钮，默认所有查询条件。选择对应的查询条件，则显示符合结果的数据。查询出的数据，按照“提交日期”顺序排列。

- 重置：重置所有查询条件。

- 审批：跳转“明细审批界面”界面。

（3）查询栏字段说明

**订单成本审批查询栏字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写订单号，支持模糊查询； |
| 提单号 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写提单号，支持模糊查询； |
| 客户 | 取值形式：字符；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写客户，支持模糊查询； |
| 创建人 | 取值形式：文本 | 非必填；无默认值 | 手工录入；<br>填写创建人，支持模糊查询 |
| 业务日期 | 取值形式：日期 | 非必填；无默认值 | 日期选择框；<br>不选择默认全部日期 |
| 提交日期 | 取值形式：日期 | 非必填；无默认值 | 日期选择框；<br>不选择默认全部日期 |

（4）审批列表明细字段说明

**订单成本审批列表明细字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：20 | 非必填；无默认值；不可修改 | 根据查询条件，自动带出；<br>无查询条件，默认全部（仅展示订单结算明细行状态为“已提交”、“业务审批通过”的数据） |
| 客户 | 取值形式：字符；长度或格式：60 | 非必填；无默认值；不可修改 | 根据查询条件，自动带出；<br>无查询条件，默认全部 |
| 创建人 | 取值形式：文本；长度或格式：60 | 非必填；无默认值；不可修改 | 根据查询条件，自动带出；<br>无查询条件，默认全部 |
| 业务日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 根据查询条件，自动带出；<br>无查询条件，默认全部 |
| 应收合计(本位币) | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询条件，自动带出；<br>无查询条件，默认全部 |
| 应付合计(本位币) | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询条件，自动带出；<br>无查询条件，默认全部 |
| 预计利润 | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询条件，自动带出；<br>无查询条件，默认全部 |
| 提交日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 根据查询条件，自动带出；<br>无查询条件，默认全部 |
| 审批截止日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 日期选择框；<br>不选择默认全部日期 |
| 提单号 | 取值形式：字符；长度或格式：20 | 非必填；无默认值；不可修改 | 根据查询条件，自动带出；<br>无查询条件，默认全部 |

<a id="doc-70AD0AF66B-section-bb16ba041059"></a>
#### 1.2.9 订单成本审批明细页面

订单成本审批列表页面，点击“审批”按钮，进入该“订单成本审批明细”界面，审批明细行只展示“已提交”、“业务审批通过”状态的结算明细数据。

业务主管只能看到“已提交”状态的数据进行审批；财务只能看到“业务审批通过”状态的数据进行审批。

（1）订单成本审批明细界面

（2）按钮说明

- 返回：返回订单审批列表界面。

- 子单：调整“子单”维度的数据查看界面。

- 审批通过：弹出“审批通过确认”弹框。

- 审批拒绝：弹出“审批拒绝确认”弹框。

1. 订单成本头字段说明

**订单成本审批头字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：60 | 非必填；无默认值；不可修改 | 根据选择的订单，自动带出 |
| 提单号 | 取值形式：文本；长度或格式：60 | 非必填；无默认值；不可修改 | 根据选择的订单，自动带出 |
| 客户 | 取值形式：字符；长度或格式：60 | 非必填；无默认值；不可修改 | 根据选择的订单，自动带出 |
| 业务类型 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据选择的订单，自动带出 |
| 订单创建人 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据选择的订单，自动带出 |
| 业务日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 根据选择的订单，自动带出 |
| 审批备注 | 取值形式：文本；长度或格式：200 | 非必填；无默认值；可修改 | 可填写整单的审批意见 |

（4）审批列表明细字段说明

**订单成本审批明细字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 成本属性 | 取值形式：文本 | 非必填；无默认值；不可修改 | 1. 应收/应付，可使用漏斗筛选<br>1. 优先展示应收属性的结算明细行；然后线上付 |
| (子)单号 | 取值形式：LOV；长度或格式：20 | 必填；无默认值；不可修改 | 1. 优先按照成本属性排序；<br>1. 其次按照(子)单号顺序排序。 |
| 状态 | 取值形式：字符 | 非必填；有默认值；不可修改 | 1. 业务主管只能对“已提交”的数据进行审批；<br>1. 财务只能对“业务审批通过”的数据进行审批 |
| 结算单位 | 取值形式：LOV | 必填；无默认值；不可修改 | 订单结算行数据自动带出 |
| 成本项目 | 取值形式：LOV | 必填；无默认值；不可修改 | 订单结算行数据自动带出 |
| 数量 | 取值形式：数字 | 必填；无默认值；不可修改 | 订单结算行数据自动带出 |
| 计费单位 | 取值形式：LOV | 必填；无默认值；不可修改 | 订单结算行数据自动带出 |
| 币种 | 取值形式：LOV；长度或格式：3 | 必填；有默认值；不可修改 | 订单结算行数据自动带出 |
| 税率(%) | 取值形式：LOV | 必填；有默认值；不可修改 | 订单结算行数据自动带出 |
| 含税单价(原币) | 取值形式：数字 | 必填；无默认值；不可修改 | 订单结算行数据自动带出 |
| 总金额(原币) | 取值形式：数字 | 非必填；无默认值；不可修改 | 订单结算行数据自动带出 |
| 即期汇率 | 取值形式：数字 | 非必填；有默认值；不可修改 | 订单结算行数据自动带出 |
| 约定汇率 | 取值形式：数字 | 必填；有默认值；不可修改 | 订单结算行数据自动带出 |
| 约定汇率超<br>即期汇率(%) | 取值形式：数字 | 非必填；无默认值；不可修改 | 订单结算行数据自动带出 |
| 人民币金额 | 取值形式：数字 | 非必填；无默认值；不可修改 | 订单结算行数据自动带出 |
| 更新人 | 取值形式：文本 | 非必填；有默认值；不可修改 | 订单结算行数据自动带出 |
| 更新日期 | 取值形式：日期 | 非必填；有默认值；不可修改 | 订单结算行数据自动带出 |
| 备注 | 取值形式：文本；长度或格式：200 | 非必填；无默认值；不可修改 | 订单结算行数据自动带出 |
| 审批备注 | 取值形式：文本 | 非必填；无默认值；可修改 | 手工录入审批备注 |
| 附件 | 取值形式：超链接 | 非必填；无默认值；不可修改 | 订单结算行数据自动带出，可查看附件 |
| 应收合计(CNY) | 取值形式：数字 | 非必填；无默认值；不可修改 | 系统自动生成；汇总规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 应付合计(CNY) | 取值形式：数字 | 非必填；无默认值；不可修改 | 系统自动生成；汇总规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 利润合计(CNY) | 取值形式：数字 | 非必填；无默认值；不可修改 | 系统自动生成；计算规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |

<a id="doc-70AD0AF66B-section-e2ad66bfface"></a>
#### 1.2.10 订单成本审批通过操作

（1）订单成本审批通过界面

在订单成本明细审批界面，勾选对应的数据，点击“审批通过”，弹出“审批通过确认”弹框。

（2）按钮说明

- 确认：确认审批通过勾选的结算明细行。“已提交”状态在业务主管审批通过后，状态变为“业务审批通过”；“业务审批通过”状态在财务审批通过后，状态“财务审批通过”。确认后返回“订单成本审批明细”界面。

- 取消：取消本次审批动作，返回“订单成本审批明细”界面。

<a id="doc-70AD0AF66B-section-861482bfda44"></a>
#### 1.2.11 订单成本审批拒绝操作

（1）订单成本审批拒绝界面

在订单成本明细审批界面，勾选对应的数据，点击“审批拒绝”，弹出“审批拒绝确认”弹框。

（2）按钮说明

- 确认：确认审批拒绝勾选的结算明细行。“已提交”状态在业务主管审批拒绝后，状态变为“业务审批拒绝”；“业务审批通过”状态在财务审批拒绝后，状态“财务审批拒绝”。确认后返回“订单成本审批明细”界面。

- 取消：取消本次审批动作，返回“订单成本审批明细”界面。

<a id="doc-70AD0AF66B-section-fee529b2ba1d"></a>
#### 1.2.12 调整单列表页面

调整单列表页面，可查看、搜索所有状态的调整单。“新建”、“业务审批拒绝”、“财务审批拒绝”的调整单可进行“编辑”、“删除”、“提交”数据。“财务审批通过”的调整单可进行查看。

（1）调整单列表界面

（2）按钮说明

- 调整单新增：跳转待创建“调整单”的结算明细行列表，进行调整单创建。

- 查询：在不选择任何查询字段下，点击“查询”按钮，默认所有查询条件。选择对应的查询条件，则显示符合结果的数据。查询出的数据，按照“调整单号”倒序排列。

- 重置：重置所有查询条件。

- 编辑：对该调整单进行编辑，跳转“调整单维护”界面。

- 删除：对该调整单进行删除，弹出“删除确认”弹框。

- 提交：对该调整单提交审批，弹出“提交确认”弹框。

（3）查询栏字段说明

**调整单查询栏字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写订单号，支持模糊查询； |
| (子)单号 | 取值形式：字符；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写(子)单号，支持模糊查询； |
| 提单号 | 取值形式：字符；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写提单号，支持模糊查询； |
| 调整单号 | 取值形式：字符；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写调整单，支持模糊查询； |
| 客户名称 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写客户名称，支持模糊查询； |
| 结算单位 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写结算单位，支持模糊查询； |
| 成本项目 | 取值形式：LOV | 非必填；无默认值 | LOV选择；<br>可手工录入，支持模糊匹配 |
| 成本属性 | 取值形式：LOV | 非必填；无默认值 | LOV选择（应收/应付） |
| 币种 | 取值形式：LOV | 非必填；无默认值 | LOV选择 |
| 状态 | 取值形式：LOV | 非必填；无默认值 | LOV选择（新建/业务审批拒绝/财务审批通过） |
| 订单创建人 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写创建人，支持模糊查询 |
| 业务日期 | 取值形式：日期 | 非必填；无默认值 | 日期选择框；<br>不选择默认全部日期 |
| 创建人 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写创建人，支持模糊查询 |
| 创建日期 | 取值形式：日期 | 非必填；无默认值 | 日期选择框；<br>不选择默认全部日期 |

（4）调整单明细栏字段

只展示“新建”、“业务审批拒绝”、“财务审批拒绝”的调整单，每页显示50条

**调整单明细栏字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 调整单号 | 取值形式：字符；长度或格式：20 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 状态 | 取值形式：文本；长度或格式：20 | 非必填；有默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认“新建”、“业务审批拒绝”、“财务审批拒绝” |
| 订单号 | 取值形式：字符；长度或格式：20 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 提单号 | 取值形式：字符；长度或格式：20 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 订单创建人 | 取值形式：文本；长度或格式：60 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 业务日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| (子)单号 | 取值形式：字符；长度或格式：20 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 结算单位 | 取值形式：LOV | 必填；无默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 成本项目 | 取值形式：LOV | 必填；无默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 成本属性 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 数量 | 取值形式：数字 | 必填；无默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 计费单位 | 取值形式：LOV | 必填；无默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 税率(%) | 取值形式：LOV | 必填；有默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 含税单价(原币) | 取值形式：数字 | 必填；无默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 总金额(原币) | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 币种 | 取值形式：LOV；长度或格式：3 | 必填；有默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 即期汇率 | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 约定汇率 | 取值形式：数字 | 必填；有默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 约定汇率超<br>即期汇率(%) | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 人民币金额 | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 更新人 | 取值形式：文本 | 非必填；有默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 更新日期 | 取值形式：日期 | 非必填；有默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |

<a id="doc-70AD0AF66B-section-d807501f8a9b"></a>
#### 1.2.13 调整单编辑界面

（1）调整单编辑界面

调整单列表页面，点击“编辑”按钮，跳转至调整单维护界面。

（2）按钮说明

- 返回：取消编辑，返回“调整单列表”界面。

- 保存：保存该调整单，跳转至“调整单列表”界面，不改变该调整单状态，根据该编辑操作用户，更新“更新人”、“更新日期”字段。保存时需校验必填字段是否按要求填写。

- 保存并提交：保存并提交该调整单。跳转至“调整单列表”界面，该调整单状态为“已提交”，根据该编辑操作用户，更新“更新人”、“更新日期”字段。保存提交时需校验必填字段是否按要求填写。

（3）调整单编辑头字段说明

**调整单编辑头字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 调整单号 | 取值形式：字符；长度或格式：14 | 非必填；无默认值；不可修改 | 根据选择的调整单，自动带出； |
| 单据类型 | 取值形式：文本 | 非必填；无默认值；不可修改 |  |
| 客户 | 取值形式：文本；长度或格式：60 | 非必填；无默认值；不可修改 | 根据选择的调整单，自动带出 |
| 提单号 | 取值形式：文本；长度或格式：60 | 非必填；无默认值；不可修改 | 根据选择的调整单，自动带出 |
| 更新人 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据操作人，自动带出 |
| 更新日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 根据当前日期，自动带出 |
| 状态 | 取值形式：文本；长度或格式：20 | 非必填；无默认值；不可修改 | 根据选择的调整单，自动带出； |

（4）调整单明细信息字段说明

**调整单明细信息字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：20 | 非必填；无默认值；不可修改 | 根据选择的调整单，自动带出调整单的单号 |
| (子)单号 | 取值形式：字符；长度或格式：14 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出调整单的单号 |
| 结算单位 | 取值形式：LOV | 必填；有默认值；可修改 | 根据选择的调整单，自动带出，可修改编辑 |
| 成本项目 | 取值形式：LOV | 必填；有默认值；可修改 | 根据选择的调整单，自动带出，可修改编辑。调整单业务类型可以选择所有的成本项目 |
| 成本属性 | 取值形式：LOV | 必填；有默认值；可修改 | 1、根据选择的调整单，自动带出，可修改编辑。<br>2、LOV选择(应收/应付) |
| 数量 | 取值形式：数字 | 必填；有默认值；可修改 | 根据选择的调整单，自动带出，可修改编辑 |
| 计费单位 | 取值形式：文本 | 必填；有默认值；可修改 | 根据选择的调整单，自动带出，可修改编辑 |
| 税率(%) | 取值形式：LOV | 必填；有默认值；可修改 | 1. 根据选择的调整单，自动带出，可修改编辑；<br>2、LOV选择税率。 |
| 含税单价(原币) | 取值形式：数字 | 必填；有默认值；可修改 | 根据选择的调整单，自动带出，可修改编辑 |
| 总金额(原币) | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 币种 | 取值形式：LOV；长度或格式：3 | 必填；有默认值；可修改 | 1. 根据选择的调整单，自动带出，可修改编辑<br>1. LOV选择币种，LOV展示2列（币种代码和币种名称） |
| 即期汇率 | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 约定汇率 | 取值形式：数字 | 必填；有默认值；可修改 | 根据选择的调整单，自动带出 |
| 约定汇率超<br>即期汇率(%) | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 人民币金额 | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 调账责任人 | 取值形式：LOV | 非必填；有默认值；可修改 | 根据选择的调整单，自动带出，可修改编辑 |
| 调账原因 | 取值形式：文本；长度或格式：200 | 非必填；有默认值；可修改 | 根据选择的调整单，自动带出，可修改编辑 |
| 调整种类 | 取值形式：LOV | 非必填；有默认值；可修改 | 根据选择的调整单，自动带出，可修改编辑 |
| 附件 | 取值形式：超链接 | 非必填；无默认值；不可修改 | 根据选择的调整单自动带出；条件必填规则见[调整单编辑界面](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-d807501f8a9b)。 |

调整种类为 9～12 时，附件必填；其他调整种类下附件非必填。

（5）原始结算明细信息字段说明

**调整单原始结算明细字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：20 | 非必填；无默认值；不可修改 | 根据选择的调整单，自动带出调整单的单号 |
| 订单行号 | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据选择的调整单，自动带出调整单的单号 |
| (子)单号 | 取值形式：字符；长度或格式：14 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 结算单位 | 取值形式：文本；长度或格式：60 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 成本项目 | 取值形式：文本；长度或格式：60 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 成本属性 | 取值形式：文本；长度或格式：60 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 数量 | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 计费单位 | 取值形式：文本 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 税率(%) | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 含税单价(原币) | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 总金额(原币) | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 币种 | 取值形式：字符；长度或格式：3 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 即期汇率 | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 约定汇率 | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 约定汇率超<br>即期汇率(%) | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 人民币金额 | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 建档人 | 取值形式：文本 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 更新日期 | 取值形式：日期 | 非必填；有默认值；不可修改 | 根据选择的调整单，自动带出 |
| 备注 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据选择的调整单，自动带出 |
| 附件 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据选择的调整单，自动带出，超链接 |

<a id="doc-70AD0AF66B-section-0638bd161e7d"></a>
#### 1.2.14 调整单新增页面

调整单新增列表页面，可查看、搜索所有审批通过的订单结算明细行信息，并勾选订单结算明细行录入应收、应付费用。

（1）订单结算明细行列表界面

（2）按钮说明

- 调整单列表：跳转“调整单列表”页面。

- 查询：在不选择任何查询字段下，点击“查询”按钮，默认所有查询条件。选择对应的查询条件，则显示符合结果的数据。查询出的数据，按照“订单号”、“业务日期”倒序排列。

- 新增：跳转“调整单新增”界面。

（3）查询栏字段说明

**调整单新增查询栏字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：20 | 非必填；无默认值 | 手工录入；<br>填写订单号，支持模糊查询； |
| (子)单号 | 取值形式：字符；长度或格式：20 | 非必填；无默认值 | 手工录入；<br>填写(子)单号，支持模糊查询； |
| 客户 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写客户名称，支持模糊查询； |
| 结算单位 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写结算单位，支持模糊查询； |
| 提单号 | 取值形式：字符；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写提单号，支持模糊查询； |
| 成本项目 | 取值形式：LOV | 非必填；无默认值 | LOV选择；<br>可手工录入，支持模糊匹配 |
| 成本属性 | 取值形式：LOV | 非必填；无默认值 | LOV选择（应收/应付） |
| 币种 | 取值形式：LOV | 非必填；无默认值 | LOV选择 |
| 订单创建人 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写创建人，支持模糊查询 |
| 业务日期 | 取值形式：日期 | 非必填；无默认值 | 日期选择框；<br>不选择默认全部日期 |
| 成本行创建人 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 手工录入；<br>填写创建人，支持模糊查询 |
| 成本行创建日期 | 取值形式：日期 | 非必填；无默认值 | 日期选择框；<br>不选择默认全部日期 |

（4）待创建调整单明细栏字段

只展示“财务审批通过”的结算明细行数据，每页显示50条

**待创建调整单明细栏字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：20 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 提单号 | 取值形式：字符；长度或格式：20 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 客户 | 取值形式：文本；长度或格式：60 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 订单创建人 | 取值形式：文本；长度或格式：60 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 业务日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| (子)单号 | 取值形式：字符；长度或格式：20 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 结算单位 | 取值形式：LOV | 必填；无默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 成本项目 | 取值形式：LOV | 必填；无默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 成本属性 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 数量 | 取值形式：数字 | 必填；无默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 计费单位 | 取值形式：LOV | 必填；无默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 税率(%) | 取值形式：LOV | 必填；有默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 含税单价(原币) | 取值形式：数字 | 必填；无默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 总金额(原币) | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 币种 | 取值形式：LOV；长度或格式：3 | 必填；有默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 即期汇率 | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 约定汇率 | 取值形式：数字 | 必填；有默认值；可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 约定汇率超<br>即期汇率(%) | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 人民币金额 | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 更新人 | 取值形式：文本 | 非必填；有默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |
| 更新日期 | 取值形式：日期 | 非必填；有默认值；不可修改 | 根据查询调整带出；<br>无查询条件默认全部 |

<a id="doc-70AD0AF66B-section-013bfcd4a338"></a>
#### 1.2.15 调整单编辑界面

（1）调整单编辑界面

调整单列表页面，点击“编辑”按钮，跳转至调整单维护界面。

（2）按钮说明

- 返回：取消编辑，返回“调整单列表”界面。

- 保存：保存该调整单，跳转至“调整单列表”界面，不改变该调整单状态，根据该编辑操作用户，更新“更新人”、“更新日期”字段。保存时需校验必填字段是否按要求填写。

- 保存并提交：保存并提交该调整单。跳转至“调整单列表”界面，该调整单状态为“已提交”，根据该编辑操作用户，更新“更新人”、“更新日期”字段。保存提交时需校验必填字段是否按要求填写。

（3）调整单编辑头字段说明

**调整单新增编辑头字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：14 | 必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出； |
| 提单号 | 取值形式：文本；长度或格式：60 | 非必填；无默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 客户 | 取值形式：文本 | 必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 业务类型 | 取值形式：文本 | 必填；有默认值；不可修改 |  |
| 订单创建人 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 业务日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 调整单号 | 取值形式：字符；长度或格式：14 | 必填；有默认值；不可修改 | 调整单保存后，自动生成。<br>规则：ADJ+6年月日(YYMMDD)+5位流水； |
| 状态 | 取值形式：文本；长度或格式：20 | 必填；有默认值；不可修改 | 创建调整单默认“新建”； |
| 审批备注 | 取值形式：文本 | 非必填；无默认值；不可修改 |  |

（4）调整单明细信息字段说明

**调整单新增明细信息字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| (子)单号 | 取值形式：字符；长度或格式：14 | 必填；有默认值；不可修改 | 调整单保存后，自动生成。为调整单头上的单号 |
| 结算单位 | 取值形式：LOV | 必填；有默认值；可修改 | 1、根据选择的订单结算明细行，自动带出<br>2、可修改编辑<br>3、LOV选择 |
| 成本项目 | 取值形式：LOV | 必填；有默认值；可修改 | 1、根据选择的订单结算明细行，自动带出<br>2、可修改编辑<br>3、LOV选择(调整单类型可以选择所有的成本项目) |
| 成本属性 | 取值形式：LOV | 必填；有默认值；可修改 | 1、根据选择的订单结算明细行，自动带出<br>2、可修改编辑<br>3、LOV选择(应收/应付) |
| 数量 | 取值形式：数字 | 必填；有默认值；可修改 | 1、根据选择的订单结算明细行，自动带出<br>2、可修改编辑<br>3、手工录入 |
| 计费单位 | 取值形式：文本 | 必填；有默认值；可修改 | 1、根据选择的订单结算明细行，自动带出<br>2、可修改编辑<br>3、手工录入 |
| 币种 | 取值形式：LOV；长度或格式：3 | 必填；有默认值；可修改 | 1、根据选择的订单结算明细行，自动带出<br>2、可修改编辑<br>3、LOV选择，LOV显示2列（币种代码和币种名称） |
| 税率(%) | 取值形式：LOV | 必填；有默认值；可修改 | 1、根据选择的订单结算明细行，自动带出<br>2、可修改编辑<br>3、LOV选择 |
| 含税单价(原币) | 取值形式：数字 | 必填；有默认值；可修改 | 1、根据选择的订单结算明细行，自动带出<br>2、可修改编辑<br>3、手工录入 |
| 总金额(原币) | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行自动带出；计算与保存规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 即期汇率 | 取值形式：数字 | 非必填；有默认值；不可修改 | 1、根据选择的订单结算明细行，自动带出 |
| 约定汇率 | 取值形式：数字 | 必填；有默认值；可修改 | 1. 根据选择的订单结算明细行，自动带出；<br>2、可修改编辑<br>3、手工录入 |
| 约定汇率超<br>即期汇率(%) | 取值形式：数字 | 非必填；无默认值；不可修改 | 系统自动生成；计算规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 人民币金额 | 取值形式：数字 | 非必填；无默认值；不可修改 | 系统自动生成；计算规则见[订单成本金额与汇率规则](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-f3a6b8c29d10)。 |
| 更新人 | 取值形式：文本 | 非必填；有默认值；不可修改 | 保存操作时，根据当前用户自动带出 |
| 更新日期 | 取值形式：日期 | 非必填；有默认值；不可修改 | 保存操作时，根据当前系统日期自动带出 |
| 调账责任人 | 取值形式：LOV | 必填；无默认值；可修改 | 1、LOV选择；<br>2、只能选择到该主体下所有的用户。 |
| 调账原因 | 取值形式：文本；长度或格式：200 | 非必填；无默认值；可修改 | 手工录入调账原因 |
| 调账种类 | 取值形式：LOV | 非必填；有默认值；可修改 | 根据选择的订单结算明细行自动带出，允许选择修改；附件条件必填规则见[调整单新增编辑界面](PRD.高捷物流系统一期.第024篇.财务结算-订单成本管理.70AD0AF66B.md#doc-70AD0AF66B-section-013bfcd4a338)。 |
| 附件 | 取值形式：超链接 | 非必填；无默认值；可修改 | 1、手工上传附件，文件、图片格式<br>2、大小≤20M。 |

注：调账种类包括：1. 漏录入；2. 错误录入-多录；3. 错误录入-少录；4. 错误录入-抬头；5. 错误录入-币种；6. 错误录入-税率；7. 错误录入-费用项目；8. 人为原因调整；9. 不可抗力争议调整-调减；10. 正常差异调整-调减；11. 正常差异调整-调减；12. 新增项目调整-调减。

调整种类为 9～12 时，附件必填；其他调整种类下附件非必填。

（5）原始结算明细信息字段说明

**调整单新增原始结算明细字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| (子)单号 | 取值形式：字符；长度或格式：14 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 结算单位 | 取值形式：文本；长度或格式：60 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 成本项目 | 取值形式：文本；长度或格式：60 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 成本属性 | 取值形式：文本；长度或格式：60 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 数量 | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 计费单位 | 取值形式：文本 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 币种 | 取值形式：字符；长度或格式：3 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 税率(%) | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 含税单价(原币) | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 总金额(原币) | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 即期汇率 | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 约定汇率 | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 约定汇率超<br>即期汇率(%) | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 人民币金额 | 取值形式：数字 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 更新人 | 取值形式：文本 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 更新日期 | 取值形式：日期 | 非必填；有默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 备注 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据选择的订单结算明细行，自动带出 |
| 附件 | 取值形式：超链接 | 非必填；无默认值；可修改 | 1、手工上传附件，文件、图片格式<br>2、大小≤20M。 |

<a id="doc-70AD0AF66B-section-a961ca228adc"></a>
#### 1.2.16 订单成本汇总查询页面

（1）订单成本汇总查询界面

（2）按钮说明

- 查询：在不选择任何查询字段下，点击“查询”按钮，默认所有查询条件。选择对应的查询条件，则显示符合结果的数据。查询出的数据，按照“业务日期”倒序排列（业务日期为各订单服务完成的最终日期）。

- 重置：重置所有查询条件。

- 查看：跳转“订单成本查询”界面。

（3）订单成本汇总查询栏字段说明

**订单成本汇总查询栏字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：60 | 非必填；无默认值 | 1、手工录入订单号；<br>2、支持模糊搜索 |
| 提单号 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 1、手工录入提单号；<br>2、支持模糊搜索 |
| 客户名称 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 1、手工录入客户名称；<br>2、支持模糊搜索 |
| 创建人 | 取值形式：文本 | 非必填；无默认值 | 1、手工录入创建人；<br>2、支持模糊搜索 |
| 业务日期 | 取值形式：日期 | 非必填；无默认值 | 1、日期选择框；<br>2、不选择默认所有调教 |

（4）订单成本汇总查询明细字段说明

**订单成本汇总查询明细字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：60 | 非必填；无默认值；不可修改 | 1、根据查询条件自动带出；<br>2、无查询条件默认全部 |
| 客户名称 | 取值形式：文本；长度或格式：60 | 非必填；无默认值；不可修改 | 1、根据查询条件自动带出；<br>2、无查询条件默认全部 |
| 创建人 | 取值形式：文本 | 非必填；无默认值；不可修改 | 1、根据查询条件自动带出；<br>2、无查询条件默认全部 |
| 业务日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 1、根据查询条件自动带出；<br>2、无查询条件默认全部 |
| 应收合计<br>(本位币) | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询条件自动带出订单上的应收合计(CNY) |
| 应付合计<br>(本位币) | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询条件自动带出订单上的应付合计(CNY) |
| 预计利润(本位币) | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询条件自动带出订单上的预计利润(CNY) |
| 提单号 | 取值形式：文本；长度或格式：60 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件时默认展示全部。 |

<a id="doc-70AD0AF66B-section-3a0ba30fd548"></a>
#### 1.2.17 订单成本查询页面

（1）订单成本明细子单查看界面

（2）特殊说明

字段与“订单成本维护列表”一致，仅可查看，不可编辑。

<a id="doc-70AD0AF66B-section-f391565492c5"></a>
#### 1.2.18 订单成本明细查询页面

（1）订单成本明细查询界面

（2）按钮说明

- 查询：在不选择任何查询字段下，点击“查询”按钮，默认所有查询条件。选择对应的查询条件，则显示符合结果的数据。查询出的数据，按照“订单号”倒序、“财务审批”、“业务审批日期”倒序排列。

- 重置：重置所有查询条件。

- 导出：批量勾选数据后，可勾选数据导出、也可按查询条件。

（3）订单成本明细查询栏字段说明

**订单成本明细查询栏字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：60 | 非必填；无默认值 | 1、手工录入订单号；<br>2、支持模糊搜索 |
| 提单号 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 1、手工录入提单号；<br>2、支持模糊搜索 |
| 客户名称 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 1、手工录入客户名称；<br>2、支持模糊搜索 |
| 结算单位 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 1、手工录入结算单位；<br>2、支持模糊搜索 |
| 状态 | 取值形式：LOV | 非必填；无默认值 | LOV选择结算明细行状态 |
| 成本项目 | 取值形式：文本；长度或格式：60 | 非必填；无默认值 | 1、手工录入成本项目；<br>2、支持模糊搜索 |
| 成本属性 | 取值形式：LOV | 非必填；无默认值 | LOV选择（应收/应付） |
| 币种 | 取值形式：LOV | 非必填；无默认值 | 1、LOV选择<br>2、LOV展示2列（币种代码、币种名称） |
| 订单创建人 | 取值形式：文本 | 非必填；无默认值 | 1、手工录入创建人；<br>2、支持模糊搜索 |
| 业务日期 | 取值形式：日期 | 非必填；无默认值 | 1、日期选择框；<br>2、不选择默认所有日期 |
| 成本创建人 | 取值形式：文本 | 非必填；无默认值 | 1、手工录入创建人；<br>2、支持模糊搜索 |
| 成本创建日期 | 取值形式：日期 | 非必填；无默认值 | 1、日期选择框；<br>2、不选择默认所有日期 |
| 业务审批日期 | 取值形式：日期 | 非必填；无默认值 | 1、日期选择框；<br>2、不选择默认所有日期 |
| 财务审批日期 | 取值形式：日期 | 非必填；无默认值 | 1、日期选择框；<br>2、不选择默认所有日期 |

（4）订单成本明细字段说明

**订单成本明细字段及场景约束：**

| 字段 | 取值与格式 | 字段属性 | 规则说明 |
| --- | --- | --- | --- |
| 订单号 | 取值形式：字符；长度或格式：60 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 状态 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 客户 | 取值形式：文本；长度或格式：60 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 订单创建人 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 业务日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| (子)单号 | 取值形式：字符 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 结算单位 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 成本项目 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 成本属性 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 含税单价<br>(原币) | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 总金额(原币) | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 币种 | 取值形式：字符 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 即期汇率 | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 约定汇率 | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 约定汇率超即期汇率(%) | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 人民币金额 | 取值形式：数字 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 提单号 | 取值形式：字符 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 成本创建人 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 成本创建日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 备注 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 审批备注 | 取值形式：文本 | 非必填；无默认值；不可修改 | 根据查询条件自动带出；无查询条件默认全部 |
| 业务审批日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 结算明细行业务审批的日期 |
| 财务审批日期 | 取值形式：日期 | 非必填；无默认值；不可修改 | 结算明细行财务审批的日期 |

<a id="doc-70AD0AF66B-section-036da482966a"></a>
#### 1.2.19 结算费用新增

| 场景名称 | 财务结算管理-结算费用新增 |
| --- | --- |
| 范围/概述 | 选择订单数据，点击新增应收/新增应付，进行应收/应付的录入，选择(子)单号、结算单位、成本项目、数量、计费单位、税率(%)、含税单价(原币)、约定汇率、备注、上传附件，完成应收/应付成本的新增 |
| 主要参与者 | 业务 |
| 前置条件 | 进入结算成本管理->订单成本列表，选择需要录入应收应付的订单，点击“成本编辑”按钮，进入此页面 |
| 业务结果 | 保存后生成状态为“新建”的成本行；提交后进入主管、财务审批。校验失败时不保存并提示原因。 |
| 主业务流程 | 选择订单，进入“订单成本维护”页面，选择(子)单号、结算单位、成本项目、数量、计费单位、税率(%)、含税单价(原币)、约定汇率、备注、上传附件。<br>1. (子)单号：<br>- 下拉选择，只能选到主体为该公司的且和该订单关联的订单、分单/服务单。<br>- 未保存前，可重新录入修改。<br>1. 结算单位：<br>- 下拉选择，所有与该公司关联的客户，支持手工录入模糊匹配。<br>- 未保存前，可重新录入修改。<br>1. 成本项目<br>- 下拉选择，只能选择到订单头“业务类型”关联的“成本项”。需从“业务类型与成本项”关系中选择。<br>- 未保存前，可重新录入修改。<br>1. 数量<br>- 手工录入计费的数量<br>- 未保存前，可重新录入修改。<br>1. 计费单位<br>- 手工录入计费单位<br>- 未保存前，可重新录入修改。<br>1. 币种<br>- 下拉选择，默认CNY人民币，可修改为其他币种;<br>- 未保存前，可重新录入修改。<br>1. 税率(%)<br>- 下拉选择，默认0税率；不含税则选择0税率。<br>- 未保存前，可重新录入修改。<br>1. 含税单价(原币)<br>- 手工录入含税单价(原币)<br>- 未保存前，可重新录入修改。<br>1. 约定汇率<br>- 系统根据所选币种和该成本行创建日期自动带出即期汇率，并将其作为默认约定汇率。<br>- 未保存前，可重新录入修改。<br>1. 备注<br>- 手工输入该行成本的备注<br>- 未保存前，可重新录入修改。<br>1. 附件<br>- 上传成本行的附件<br>- 未保存前，可重新录入修改。<br>应收、应付栏各显示5行数据，支持下拉查看。<br>点击“保存”完成该行成本的录入。<br>- 保存校验：存在必填字段未填写时，无法保存。<br>- 保存成功：该成本行状态变为“新建”，并显示在订单成本维护页面。<br>- 取消：不保存本次录入，停留在订单成本维护页面。<br>- 删除：删除当前尚未提交的成本行。 |
| 业务分支 | 勾选对应数据，点击“提交”，提交给主管、财务审批<br>- 提交后，该行数据状态变为“已提交”，无法继续编辑。<br>业务部门审批<br>- 业务部门审批通过，该行数据状态变为“业务审批通过”，等待财务进行审批<br>- 业务部门审批拒绝，该行数据状态变为“业务审批拒绝”，该行数据业务员可以进行编辑。<br>财务部门审批，财务部门在业务部门审批通过的数据上继续审批<br>- 财务部门审批通过，该行数据状态变为“财务审批通过”，该数据同步金蝶系统；同步的入账日期为“业务部门审批通过的日期”。<br>- 财务部门审批拒绝，该行数据状态变为“财务审批拒绝”，该行数据业务员可以进行编辑。 |
| 特殊需求 | 提交及逐级审批的消息触发、接收角色和模板见[结算成本通知规则](PRD.高捷物流系统一期.第030篇.消息推送.5F950AB3B1.md#doc-5F950AB3B1-section-817df2a7743c)。 |
| 关联功能 | 1.调整单录入<br>2.应收/应付对账单管理 |
