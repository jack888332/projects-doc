# 通用业务 PRD 与变更需求表达指南

## 1. 这份指南解决什么问题？

### 1.1 适用范围

本指南用于编写或改写各业务领域的软件产品需求文档（PRD），适用于：

- 新增普通 PRD 或章节。
- 优化既有 PRD 的结构和表达。
- 整理已有页面、流程、业务工作项、后台自动作业、接口或数据规则的变更需求。

本文件所在目录以及文中的物流、账单、金额等例子，不限制指南的适用范围。实际编写时，应替换为当前产品和业务领域使用的正式名称。

### 1.2 交付目标

整理后的需求应让产品、业务、研发和测试沿着同一条思路读懂：

1. 为什么提出需求，要解决什么具体问题。
2. 谁在什么位置发起，业务经过哪些主要环节。
3. 页面、业务工作项、后台自动作业、接口、字段、计算、状态或权限怎样变化。
4. 成功路径、业务分支、异常、兼容和存量数据怎样处理。
5. 哪些规则已经确定，哪些问题仍需产品经理确认。

最终交付物包括需求正文和独立的逻辑问题清单。需求正文同时作为研发实现和测试检查的依据，不再复制一章“验收标准”。若正文不能直接检查，应补全规则和边界，而不是另写一套口径。

### 1.3 不可破坏的原则

- **逻辑完整**：调整结构、顺序、标题、措辞或图示时，不得改变已经确认的业务口径。
- **先总后分**：先交代目的和全貌，再逐步展开场景、规则、数据与边界。
- **唯一主描述**：一条规则只在一个位置完整定义，其他位置通过编号或链接引用。
- **具体可执行**：写清条件、对象、处理和可观察结果，避免让研发或测试猜测。
- **术语一致**：同一对象、字段、状态和操作始终使用同一名称。
- **问题透明**：无法形成唯一实现结果的问题必须显式列出，不得自行选择口径并静默覆盖。

本指南中，“必须”表示缺失后需求无法唯一实现或检查；“应”表示通常需要遵守，确有理由时可以说明例外；“可以”表示按内容复杂度选用。

### 1.4 统一术语

| 术语 | 含义 |
| --- | --- |
| 普通 PRD | 完整描述新增能力或既有能力的需求，不以修改前后差异为主线 |
| 变更需求 | 基于已确认或已上线能力，明确说明修改前后差异及影响的需求 |
| 参与方 | 用户角色、业务系统、后台自动作业或外部服务 |
| 成功主流程（Happy Flow） | 业务从起点成功到达终点的主要路径 |
| 业务分支 | 可预期且能够按已确认规则得到业务结果的情况，即使结果并不理想，例如缺货转人工复核 |
| 异常流程 | 系统无法按预定规则完成处理的情况，例如数据不合法、执行失败、超时、并发冲突、回退、重试或补偿 |
| 业务工作项 | 由人员领取、分派或处理，并具有处理人、状态和完成条件的业务对象，例如审批待办或复核工作项 |
| 后台自动作业 | 由系统按时间、事件或条件自动执行的处理，例如定时扫描或批量归集；不包括业务工作项 |
| 存量数据与已生效结果 | 新规则生效前已经产生的数据，以及已经完成、锁定或对外生效的结果 |

## 2. 怎样使用这份指南？

### 2.1 先按任务选读

| 任务 | 必读内容 | 使用模板 |
| --- | --- | --- |
| 新写普通 PRD | 第 3～9 章和第 11 章 | 第 10.1 节 |
| 改写既有普通 PRD | 第 3～9 章和第 11 章，重点建立现状基线并检查改写前后一致性 | 第 10.1 节 |
| 整理变更需求 | 第 3～9 章和第 11 章，重点阅读第 4.3、6.9 和 8.2 节 | 第 10.2 节 |

### 2.2 统一执行七步

| 步骤 | 主要输入 | 关键动作 | 产出 | 对应章节 |
| --- | --- | --- | --- | --- |
| 1. 明确目的与边界 | 原始需求、关联资料、业务口径 | 说明缘由、对象、范围、输入和输出；既有需求建立基线 | 需求定位卡 | 第 3 章 |
| 2. 选择路线并搭建骨架 | 需求定位卡 | 选择普通 PRD 或变更需求路线，确定唯一主描述位置 | 章节目录 | 第 4 章 |
| 3. 建立业务全貌 | 参与方、对象、输入输出、主要环节 | 先用文字概括，复杂时补 D2 总览图或对象关系图 | 业务全貌 | 第 5 章 |
| 4. 补全详细需求 | 已确认规则、字段、原型、状态和外部契约 | 先盘点规则，再按需绘制 PlantUML，并补数据示例 | 可实现、可检查的正文 | 第 6 章 |
| 5. 优化结构与措辞 | 完整需求初稿 | 调整衔接，统一术语，删除无效和含糊表达 | 易读正文 | 第 7 章 |
| 6. 检查逻辑与一致性 | 正文、图、原型、示例、关联 PRD | 查冲突、缺失、歧义、存量和多载体口径 | 问题初稿 | 第 8 章 |
| 7. 完成交付 | 正文和问题初稿 | 区分评审稿与正式稿，输出逻辑问题清单 | PRD 正文 + 问题清单 | 第 9 章 |

执行顺序和成稿顺序不同：编写时先确认详细规则，再据此绘制 PlantUML；成稿时可以把场景图放在规则之前，帮助读者先看全貌。不得用尚未确认的图反推业务规则。第 3 章的需求定位卡默认是编写底稿，其目的、范围和上下游信息应转写进正文；普通 PRD 的改写基线默认作为审查依据，只有影响读者理解时才进入正文；变更需求的基线必须进入正文。

## 3. 第一步：明确需求目的与边界

### 3.1 先用简洁语义说明缘由

开始写正文前，先回答：

- 现有业务怎样处理，出现了什么可观察的问题或风险？
- 本次需求要解决什么问题，不解决会产生什么业务影响？
- 谁会使用或受到影响，处理的核心业务对象是什么？
- 上游提供什么输入，本需求输出什么结果，结果交给谁继续使用？
- 哪些内容在本需求范围内，哪些明确不在范围内？
- 哪些规则由本需求完整定义，哪些只引用其他章节？

需求目的应直接写“缘由 + 目标结果”，不写“优化体验”“完善能力”等抽象结论。内容较简单时，用一至两段说清即可。

### 3.2 形成需求定位卡

| 项目 | 需要填写的内容 |
| --- | --- |
| 需求目的 | 要解决的具体问题和预期业务结果 |
| 目标用户或参与方 | 使用者、受影响角色、系统、后台自动作业或外部服务 |
| 核心对象 | 本需求处理的业务对象或记录 |
| 范围 | 本次纳入和明确排除的内容 |
| 上游输入 | 来源、触发条件、前置状态或已有配置 |
| 下游输出 | 页面结果、数据结果、状态、通知或后续流程 |
| 规则归属 | 本章完整说明的规则，以及只引用的外部规则 |
| 待确认事项 | 暂时无法形成唯一口径的问题；搭建骨架时再补 `Q-xx` 编号 |

无法填清需求目的、输入、输出或规则归属时，先补齐边界，不要直接罗列字段或调整原型。

### 3.3 改写和变更需求必须建立基线

基线用于证明“修改前表现”以哪个已确认版本为准，至少包括：

- 需求编号或变更编号。
- 关联 PRD 的文件、章节或规则位置。
- 关联原型或页面版本。
- 当前线上版本或最近确认版本。
- 基线日期和能够证明现状的资料。

无法确认基线，且不同基线会产生不同修改结果时，应列为阻断问题，不能凭记忆描述现状。

## 4. 第二步：选择需求路线并搭建骨架

### 4.1 所有 PRD 共用的信息顺序

无论选择哪条路线，读者都应依次获得以下信息：

1. 为什么做，以及做什么、不做什么。
2. 谁在什么产品、系统或业务位置处理。
3. 业务总体怎样从输入走到输出。
4. 各场景分别怎样处理。
5. 页面、字段、计算、状态、权限和外部契约有哪些约束。
6. 异常、兼容、存量数据和已生效结果怎样处理。
7. 本需求的结果怎样进入下游。

简单需求可以合并相邻小节，但必需信息不能删除。不得先讲异常再补成功主流程，也不得在业务对象尚未解释时直接堆放字段名、状态名或技术表名。

### 4.2 普通 PRD 的标准骨架

普通 PRD 按以下顺序组织，模板必须与此一致：

1. **需求目的与范围**：缘由、目标、纳入和排除范围。
2. **业务全貌**：参与方、核心对象、输入、主要环节和输出。
3. **成功主流程**：按业务发生顺序说明主要成功路径。
4. **分场景规则**：展开正常条件下的不同处理结果。
5. **业务落点与操作**：按需说明页面、业务工作项、后台自动作业、接口或集成事件的处理位置和操作。
6. **字段、计算、状态与权限**：按实际涉及的规则类型补齐。
7. **业务数据示例**：复杂或高风险规则按需提供，并引用正式规则编号。
8. **异常与边界**：失败、重复、冲突、回退、补偿、兼容和存量处理。
9. **结果与下游衔接**：说明输出由谁、在何处继续使用。

需求目的、范围、业务全貌、实际存在的核心规则、异常边界和结果衔接必须存在，但可以合并进首尾段。图、独立原型小节、字段表、状态表和数据示例按触发条件选用。

### 4.3 变更需求的标准骨架

变更需求使用标题：`变更需求：{修改对象 + 修改动作}`。不得使用“功能优化”“体验升级”“能力增强”等无法看出具体变化的标题。

按以下顺序组织：

1. **变更原因**：按“现有处理 → 触发场景 → 可观察问题或风险 → 业务影响 → 修改目的”说明缘由。
2. **变更基线**：写明需求编号、关联 PRD、原型、当前版本和基线日期，为“修改前表现”提供依据。
3. **全局变更范围**：只列本次整体纳入和排除的产品、系统、模块、用户或数据范围。
4. **变更总览**：汇总每个变更点的落点、修改前摘要、修改后摘要和分项规则链接。
5. **变更落点**：根据页面型、业务工作项型、后台自动作业型、接口或集成事件型选择第 10.2 节对应表格；一个变更涉及多种类型时分别填写。
6. **变更后业务全貌**：变更改变跨环节流程或对象关系时，按第 5 章重新说明；简单局部调整可以省略独立小节。
7. **分项变更内容**：逐个说明修改前表现、修改后表现、系统调整对象、正式规则及该点特有的相邻边界。
8. **全局生效与存量策略**：说明多个变更点共用的生效时点、新旧数据、已生效结果、重跑和失败处理；各变更点只写差异并引用该节。各变更点独立生效时，在分项内完整说明，不再重复建立全局策略。
9. **全局保持不变的规则**：只汇总跨多个变更点且容易被误改的高风险规则。
10. **结果与上下游影响**：说明输出、依赖和兼容范围。

产品需求必须直指系统改动位置和可观察结果，但不指定代码结构、技术框架、数据库表或中间件方案。已经确认且必须兼容的技术契约除外。

### 4.4 用编号串联变更点、规则、示例和问题

按下表决定是否使用编号：

| 内容 | 编号示例 | 使用要求 |
| --- | --- | --- |
| 变更点 | `CP-01` | 所有变更需求使用；具有独立业务结果、独立落点、可独立上线/回滚或独立存量策略之一时拆为独立编号 |
| 原子规则 | `R-01` | 所有变更需求使用；普通 PRD 存在跨节引用、图、示例或多条规则时使用 |
| 数据示例 | `EX-01` | 使用示例时必须编号，并引用对应规则编号 |
| 逻辑问题 | `Q-01` | 所有交付统一编号，并引用受影响的章节、字段、流程节点、变更点或规则 |

编号在一份文档内全局唯一，不按章节或变更点重新从 01 开始。编号只用于追踪，不能代替有业务含义的标题。仅为同一业务结果服务、不能独立检查的多个页面、业务工作项、后台自动作业或接口落点沿用同一 `CP-xx`。一条 `R-xx` 对应同一触发条件下的一项主要可观察结果；触发条件、责任方、结果或失败处理可以独立变化时拆分。状态表或决策表可以作为一条规则的完整载体，其每一行不必机械编号。进入评审后，不得把已删除编号分配给新内容；删除项应保留“已废弃”记录或进入变更记录。

### 4.5 避免重复定义

- 全局变更范围只写整体纳入和排除项。
- 变更落点只负责定位，不在定位表里展开完整规则。
- 每个变更点只写该点的行为变化、局部影响和特有边界。
- “全局保持不变的规则”只写跨变更点且风险较高的既有规则。
- 相同规则只保留一个完整描述位置，其他位置引用 `R-xx`，不得复制后形成两套口径。

## 5. 第三步：建立业务全貌与图形总览

### 5.1 先用文字说明全貌

先用一段话或编号步骤说明参与方、核心对象、输入、主要环节和输出，再分别判断业务流程和对象关系是否需要绘图。流程复杂度与对象关系复杂度互不依赖，可以只画一种图，也可以两种都画。

### 5.2 什么时候绘制 D2 主流程总览图？

成功路径满足以下任一条件，且编号步骤仍难以让读者一次看清全貌时，使用 D2 绘制“成功主流程总览图（Happy Flow）”：

- 主要业务环节较多，顺序、依赖或结果不容易通过编号步骤说明。
- 跨越不同业务角色、系统或处理阶段，并发生职责或数据交接；简单的一次用户操作和页面反馈不单独触发绘图。
- 存在会改变后续环节的两条及以上正常成功路径。
- 包含正常等待、异步接续、人工接续或业务循环，并影响后续成功路径。

仅包含两至三个线性步骤、由同一参与方连续完成且没有关键分支时，可以使用编号步骤代替 D2。

### 5.3 D2 主流程总览图怎样画？

- 默认展示从起点到终点的主要成功路径，不混入失败、重试和回退。
- 关键正常分支会改变后续环节时，在总览中画出分流与汇合，或另画分支总览。
- 一个节点表示读者需要理解的业务环节，名称使用“业务动作 + 结果”。
- 对业务结果有直接影响的定时作业、批处理、自动审核或异步处理可以成为节点。
- 接口调用、数据库操作、消息队列消费等实现步骤不得单独成为业务节点。
- 每个节点必须能定位到正文说明；多个简单、连续的节点可以由同一小节统一说明。
- 涉及多个参与方时，按角色、系统、后台自动作业或外部服务分组。

变更需求的流程总览至少保留一个上游入口、完整的变化路径和一个下游出口。大段未变化流程使用一个上下文节点或引用既有流程，不重复绘制整条链路。

### 5.4 什么时候绘制 D2 核心对象关系图？

当核心对象的归属、包含、引用或数量关系无法在一至三句话内无歧义地说明时，使用 D2 核心对象关系图。对象数量本身不构成绘图条件。

- 静态对象图按“业务领域”或“对象职责”划分业务分组或容器，不称为泳道；“泳道”只用于动态活动图。
- 每个对象只展示理解当前关系所需的标识、归属、关联和关键业务字段，不伪装成完整 ER 图或字段字典。
- 连线使用“包含”“归属于”“引用”等明确关系词；能确认时，用通俗文字标明一对一、一对多等数量关系。
- 无法确认的关系进入问题清单，不得猜测。
- 数据产生、传递、汇总、拆分、匹配和写回属于动态处理，应在流程正文中说明；只有达到第 6.8 节条件时才进入 PlantUML。

### 5.5 D2 图下必须补充业务说明

| 图类型 | 图下说明 |
| --- | --- |
| 主流程总览图 | 流程目的、起点和终点、参与方、主要输入输出、各环节对应的正文位置 |
| 核心对象关系图 | 分组含义、对象范围、归属或引用关系、数量关系，以及“图中字段并非完整字段清单”的说明 |

D2 图遵守 [D2 节点关系图布局规则](./layout-d2.md)。本指南只规定何时画、画什么；D2 语法、渲染和布局不列入常规检查。只有产品经理审阅发现异常或明确要求时，才执行相关校验。

## 6. 第四步：补全可实现、可检查的详细需求

### 6.1 先按场景盘点规则

先列出成功主流程、正常业务分支和异常流程，再确认每个场景的条件、参与方、对象、输入、处理和输出。完成规则、字段、状态和异常盘点后，再判断是否需要 PlantUML；不要先画详细图再补规则。

### 6.2 不同规则使用不同结构

| 规则类型 | 必须说明的内容 |
| --- | --- |
| 行为或处理规则 | 触发条件、执行者或触发方、处理对象、动作、可观察结果 |
| 字段规则 | 字段名称、业务含义、来源、格式或单位，以及本次涉及的必填、默认、选项、编辑、空值、展示或写回约束 |
| 计算或转换规则 | 输入、公式或转换关系、单位、精度、舍入、输出、空值和异常值处理 |
| 状态规则 | 变更前状态、触发条件、执行者或触发方、变更后状态、允许或禁止操作、是否可逆 |
| 权限规则 | 角色或主体、数据范围、生效条件、允许操作、禁止操作 |
| 匹配或优先级规则 | 候选对象、匹配条件、执行顺序、冲突处理、未命中处理 |
| 业务工作项规则 | 工作项名称、创建条件、关联对象、队列或处理角色、分派/领取、状态、完成条件、转派、并发和超时 |
| 后台自动作业规则 | 作业名称、触发方式、处理范围、输入、处理动作、输出、重复执行、失败和重试 |
| 接口或集成事件规则 | 提供方、调用方或消费方、触发条件、关键业务输入、业务输出、失败结果、重复请求和兼容约束 |
| 页面交互规则 | 页面位置、操作入口、前置状态、用户动作、系统反馈、结果和禁止操作 |
| 异常与边界规则 | 识别条件或信号、受影响对象、处理动作、用户反馈、重试/回退/补偿/人工处理和最终状态 |

“条件—对象—动作—结果”只适用于行为或处理规则。不得强迫字段、计算、状态、权限、业务工作项、后台自动作业或接口规则套用同一句式。不同条件产生不同结果时，拆为独立规则；多条件组合用流程图仍不直观时，使用决策表列出条件组合、结果和兜底处理。

| 规则编号 | 条件一 | 条件二 | 其他条件 | 处理结果 | 未命中/冲突处理 |
| --- | --- | --- | --- | --- | --- |
| R-xx | {条件值} | {条件值} | {按需增加} | {唯一结果} | {兜底或优先级} |

### 6.3 哪些字段必须逐项列出？

必须逐项列出：

1. 新增、删除、重命名或业务含义发生变化的字段。
2. 来源、格式、单位、必填、默认值、选项、编辑、空值、展示或脱敏规则发生变化的字段。
3. 参与计算、转换、匹配、排序、筛选、防重、状态判断、权限判断或结果写回的字段。
4. 构成本次新增或变更的表单输入、导入、导出、接口、事件或后台自动作业业务契约的字段。
5. 用于关联、快照、审计或追溯，且会影响需求结果的字段。

未发生变化、也不参与判断或数据契约的普通展示字段，不要求在正式规则中穷举；页面关键展示字段可以在原型概述中说明。

字段达到六个及以上，或跨越两个及以上业务对象或用途时，必须按业务对象或用途分组；字段较少且同属一个对象时可以不分组。通用分组包括对象识别、对象归属、业务分类、时间、数量与计算、状态与操作、来源与追溯。

字段表按需使用以下列，不为填满表格虚构规则：

| 字段/字段组 | 处理类型 | 业务含义 | 来源 | 格式/单位 | 本次约束 | 关联规则 |
| --- | --- | --- | --- | --- | --- | --- |
| {业务字段名} | 新增/修改/删除/沿用 | {供谁做什么} | {填写、读取或计算来源} | {类型、长度、单位等} | {必填、默认、选项、权限、空值、防重、展示或写回} | {R-xx} |

### 6.4 数值、计算和时间规则怎样写？

涉及数量、比例、评分、时长、容量、金额等数值时，写明输入值、单位、公式或转换关系、精度、舍入方式、有效范围、统计时间范围、空值和异常值处理，以及输出值。涉及日期时补充时区、起止边界和是否包含临界时点。

财务场景还应按需说明币种、汇率来源、正负方向和汇总范围；这些只是数值规则的一种应用，不是所有 PRD 的必填项。

### 6.5 状态和权限怎样写？

状态较多或操作受角色与状态共同限制时，使用矩阵：

| 业务对象 | 变更前状态 | 触发条件/操作 | 执行者/触发方 | 变更后状态 | 关联对象同步结果 | 允许/禁止操作 | 是否可逆 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| {对象} | {状态} | {条件或操作} | {角色} | {状态} | {其他对象的状态或数据变化} | {允许及禁止操作} | 是/否，如何恢复 |

多个对象各自有完整状态机时，分别使用状态矩阵，再单独写跨对象同步规则。权限规则必须同时说明角色、可见或可操作的数据范围、生效条件和禁止操作，不能只写“按权限控制”。

### 6.6 原型概述必须轻量化

原型概述只描述页面包含的业务区域，以及各区域的关键字段、关键操作和会影响业务结果的交互约束。

| 业务区域 | 关键业务字段 | 关键业务操作 | 关键交互约束 |
| --- | --- | --- | --- |
| {筛选区、统计区、列表区、详情区或编辑区} | {关键字段} | {查询、提交、审批等} | {会改变业务结果的条件、限制或反馈} |

不写按钮颜色、字号、间距、列宽等视觉细节，不写“输入框支持输入”等控件常识，也不指定前端框架、接口或数据库实现。已经在正式规则中定义的内容不重复抄写，使用规则编号引用。

### 6.7 页面、工作项、后台自动作业和接口都要写清业务落点

- 页面型需求定位到产品或系统、一级入口、二级入口、页面、业务区域和操作入口。
- 业务工作项型需求写明工作项名称、创建入口、处理对象、队列或角色、分派或领取方式、处理页面、状态、完成条件、转派、并发占用和超时处理。
- 后台自动作业型需求写明作业名称、触发方式、处理对象与范围、数据来源、输出、结果写入位置、失败和重复执行处理。内部触发信号归入此类。
- 接口或集成事件型需求写明提供方、调用方或消费方、业务输入输出、失败结果、重复请求和兼容范围。跨系统发布订阅或对外事件契约归入此类；内部事件具有独立业务契约时也可以使用此类。

落点让研发快速找到“改哪里”；正式规则说明“怎样改、结果是什么”。两者不能互相替代。

### 6.8 什么时候用 PlantUML 展开场景？

每个 D2 节点都必须用正文或 PlantUML 展开，但不要求简单节点也绘图。一个环节满足以下任一条件，且正文步骤、规则表或决策表仍难以清楚表达动态顺序时，使用 PlantUML 场景图：

- 判断会产生不同业务结果。
- 两个及以上参与方发生多轮交互、异步接续或职责交接，且文字难以清楚表达顺序；简单的一次请求和反馈不触发绘图。
- 存在状态流转、等待、回调、重试、回退或补偿。
- 步骤较多，纯文字容易遗漏顺序。

简单线性环节使用正文步骤即可。一张图只说明一个环节或边界清楚的场景，可按重点选择活动图、时序图或状态图。判断节点使用完整业务问题，图中的参与方、字段、状态和操作名称必须与正文一致。

图下写明场景目的、触发条件、参与方、输入输出和关键规则。存在异常时，说明异常入口并链接具体规则或小节；不存在独立异常时，不得虚构，可以写“本场景不涉及独立异常处理”。

PlantUML 只表达动态活动、交互或状态，不用于静态对象关系。[PlantUML 节点关系图布局规则](./layout-plantuml.md) 当前主要适用于类图和组件图；绘制活动图、时序图或状态图时，只复用其中通用的可读性、分组和连线原则，不套用特定图型的布局方法。PlantUML 的语法、渲染和布局不列入常规检查，仅在审阅发现异常或明确要求时检查。

### 6.9 变更需求必须说明生效和存量处理

每个会改变业务结果、数据结构、状态、权限、计算、匹配或外部契约的变更点，都必须能追溯到以下信息：

1. 生效时点或生效版本，以及适用的用户、组织、渠道、对象或数据范围。
2. 生效前已经存在但尚未完成的数据怎样处理。
3. 已完成、锁定、不可逆或已经对外生效的结果是否保持原结果。
4. 是否迁移、补算、回填、重新触发或转人工处理。
5. 重复执行怎样防止重复结果，失败后怎样重试、补偿或回滚。

多个变更点共用相同策略时，在“全局生效与存量策略”完整说明并标注关联 `CP/R`，各变更点只引用；某个变更点有差异时，只在该点补充差异。各变更点独立生效时，可以在分项内完整说明，不再建立重复的全局策略。确实不影响存量数据时，明确写“存量数据不处理”，并说明判断依据。

| 关联变更点/规则 | 数据范围/状态 | 判断时点 | 是否采用新规则 | 处理方式 | 预期结果 | 重复执行/失败处理 |
| --- | --- | --- | --- | --- | --- | --- |
| CP-xx / R-xx | 新产生数据 | {条件} | 是/否 | {处理} | {结果} | {防重、重试或补偿} |
| CP-xx / R-xx | 生效前未完成数据 | {条件} | 是/否 | {沿用、迁移或重跑} | {结果} | {处理} |
| CP-xx / R-xx | 已完成或已锁定数据 | {条件} | 是/否 | {保持、补算或人工处理} | {结果} | {处理} |

### 6.10 用业务数据示例对齐产品、研发和测试

核心规则因输入值、角色、状态、时间、匹配顺序、重复数据或存量范围不同而产生不同结果时，必须提供数据示例。每项此类核心规则至少提供一个正常场景；正文存在关键分支时，每种不同结果至少提供一个示例；正文存在高风险边界时，再提供对应边界示例。没有分支或边界时，不得为了凑数量虚构场景。

| 示例编号 | 主要验证规则 | 其他关联规则 | 场景 | 前置条件与输入数据 | 操作或系统触发 | 预期结果 | 不应发生 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| EX-01 | R-01 | R-02 | {正常/分支/边界} | {具体但虚构的数据} | {操作或触发} | {字段、数量、记录或状态结果} | {明确风险；无则填“不适用”} |

一个端到端示例可以关联多条规则，但应标明主要验证规则；同一示例可以覆盖多条规则的正常结果，不同业务结果仍需独立示例。示例使用具体且虚构的数据，不使用真实客户、账号或其他敏感信息。计算场景写出过程、单位、精度和输出；状态场景写明前后状态；匹配或防重场景写明参与判断的字段和值；权限场景写明角色和数据范围。示例只解释正式规则，不得引入新规则；规则变化后必须同步更新关联示例。

以下财务示例只演示颗粒度，不构成本指南的固定业务规则：

| 示例编号 | 主要验证规则 | 其他关联规则 | 场景 | 前置条件与输入数据 | 操作或系统触发 | 预期结果 | 不应发生 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| EX-12 | R-12 | 不适用 | 正常归集 | 附加费 AF-1001，来源为“记账单导入”，创建时间为 2026-08-05 10:00，金额为 CNY 12.50；目标账期为 2026-08-01 至 2026-08-31 | 2026-09-01 运行应收归集任务 | AF-1001 按创建时间归入该账期的应收账单，账单新增一条 CNY 12.50 明细 | 不得遗漏、归入其他账期或生成重复明细 |
| EX-13 | R-12 | 不适用 | 重复执行边界 | AF-1001 已由上一次任务成功归集，来源记录编号仍为同一值 | 再次运行相同归集任务 | 账单仍只有一条 AF-1001 明细，总额不再次增加 | 不得因任务重跑重复计费 |

以下仓储示例说明同一格式也适用于非财务领域。假设 `R-21` 已规定“报告缺货后，对应明细进入待复核，其他明细继续拣货”，`R-22` 已规定“相同缺货业务键只能存在一个未完成复核工作项”：

| 示例编号 | 主要验证规则 | 其他关联规则 | 场景 | 前置条件与输入数据 | 操作或系统触发 | 预期结果 | 不应发生 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| EX-21 | R-21 | R-22 | 缺货转人工复核 | 拣货明细 L-01，SKU-A，应拣 10 件、已拣 7 件，当前状态为“拣货中” | 拣货员报告缺货 3 件 | 系统只生成 1 个关联 L-01 的复核工作项；L-01 进入“待复核”，同一任务的其他明细继续拣货 | 不得丢失缺货数量、重复生成工作项或暂停其他明细 |
| EX-22 | R-22 | 不适用 | 重复报告边界 | L-01 已存在未完成复核工作项，缺货数量仍为 3 件 | 同一缺货业务键再次上报 | 沿用已有复核工作项，工作项数量仍为 1 | 不得重复创建待办或覆盖已提交的复核结果 |

## 7. 第五步：优化结构衔接与措辞

### 7.1 标题直接说明本节回答什么问题

标题应包含业务对象和处理动作。内容复杂时，可以使用疑问句标题或在首句提出问题来引导阅读，例如“审批被退回后，申请人怎样重新提交？”。疑问句必须由本节直接回答，不得夸张或制造焦虑；字段表、固定定义和技术附录使用直接标题即可。

### 7.2 用具体过渡句承接上下文

章节开头说明目的、对象和上下游，结尾说明结果流向。小节从主流程转到分支、从规则转到数据或从页面转到后台处理时，用一句话说明两者关系，不写“下面进行详细说明”等空过渡。

### 7.3 使用当前业务领域的惯用词

- 优先使用目标读者熟悉的业内惯用词，让首次接触该业务的读者也能理解。
- 生僻名词、系统缩写和内部简称首次出现时，先解释业务含义。
- 先写业务名称，再按需补技术字段名；数据库字段不能代替业务解释。
- 一个名词只表达一种含义，同一对象不得在不同段落随意更名。
- 一句话只表达一项主要规则；多个条件、动作或结果应拆开。

### 7.4 必要时用类比解释复杂概念

读者难以理解抽象机制时，可以先写正式规则，再用熟悉的生活或业务场景类比。一个类比只解释一个难点，不增加正文没有的新条件，最后应说明类比与实际业务对象的对应关系。

类比只帮助理解，不承担实现或测试依据；第 6.10 节的业务数据示例才用于产品、研发和测试对齐。简单规则不增加类比，避免例子比规则更难懂。

### 7.5 删除无效和含糊表达

| 类型 | 不合格表达 | 改写方式 |
| --- | --- | --- |
| 非需求表达 | 讨论过程、个人判断、未经确认的开发方案 | 移入决策记录或问题清单 |
| 不必要表达 | 控件常识、读者可以直接推出的结论 | 删除 |
| 冗余表达 | 同一规则在概述、正文和“验收标准”重复 | 保留一个主描述，其他位置引用 |
| 歧义表达 | “相关数据”“适时处理”“正常展示” | 写明具体字段、条件、时间和结果 |
| 抽象目标 | “优化体验”“完善能力”“保证准确” | 写明新增或修改什么、解决什么问题、失败怎样处理 |
| 模糊范围 | “其他逻辑不变” | 列出高风险且必须保持不变的具体规则 |

删除或精简前，先确认文字是否包含唯一条件、边界或例外，不能把业务限制一并删掉。

## 8. 第六步：检查逻辑与多载体一致性

### 8.1 通用完整性检查

逐项检查：

- 目的、范围、参与方、前置条件、触发时点、输入和输出是否完整。
- 成功主流程是否连续，业务分支是否覆盖全部条件并有兜底。
- 字段来源、格式、单位、必填、默认、空值、编辑、唯一性和写回规则是否完整。
- 数值的公式、精度、舍入、范围和时间口径是否明确。
- 状态是否可达、可退出，终态或不可逆限制能否被绕过。
- 权限是否同时覆盖角色、数据范围、允许和禁止操作。
- 失败、超时、重复、冲突、重试、回退、补偿和人工处理是否有明确结果。
- 外部接口、事件、通知或下游依赖是否有失败、重复和兼容口径。
- 存量数据与已生效结果是否会被意外覆盖或重复处理。
- 上下游章节、规则编号和交叉链接是否能够闭合。

涉及财务时，再检查金额、币种、汇率、正负方向、汇总范围、已出账或已核销结果；涉及其他领域时，替换为该领域的关键数据和不可逆结果。

### 8.2 变更需求专项检查

- 基线是否唯一且有资料支持，修改前和修改后是否形成完整对照。
- 全局范围、变更总览、落点和分项规则是否引用同一组 `CP-xx`。
- 每个变更点是否直指产品或系统、菜单、业务工作项或后台自动作业、页面区域、按钮或触发时点。
- 生效时点、新数据、未完成数据、已完成或锁定数据是否分别有处理口径。
- 迁移、回填、补算、重跑、防重、失败和回滚边界是否完整。
- 全局排除项、局部边界和保持不变的高风险规则是否各司其职、没有重复或冲突。
- 上下游、接口、导入导出和既有用户操作是否需要兼容。

### 8.3 检查所有载体是否同一口径

正文、D2、PlantUML、原型、字段表、状态表、数据示例和问题清单中的参与方、对象、名称、条件、顺序和结果必须一致。图或示例只能解释正式规则，不能成为隐藏规则的唯一来源。

### 8.4 分开判断处理状态和影响等级

| 维度 | 分类 | 判断标准 |
| --- | --- | --- |
| 处理状态 | 阻断 | 缺少必要决策，无法形成唯一、可实现、可测试的结果 |
| 处理状态 | 非阻断 | 已有唯一口径，问题只涉及补充说明、文案或不改变结果的低风险细节 |
| 影响等级 | 高/中/低 | 按对核心数据、权限与安全、外部契约、不可逆结果、主流程终态和用户范围的影响判断 |

高影响不自动等于阻断，低影响也可能因缺少唯一答案而阻断。评审稿可以在受影响位置用 `Q-xx 待确认` 占位；正式 PRD 不得遗留未关闭的阻断问题。无法及时关闭时，必须把受影响规则移出本次发布范围并明确记录。

## 9. 第七步：交付正文和逻辑问题清单

### 9.1 正文交付条件

- 需求目的、范围、核心规则、异常边界和上下游结果完整。
- 正文能够直接支持研发实现和测试检查，不另写重复的验收标准。
- 所有正式规则都有唯一主描述位置，图、原型和示例引用正式规则。
- 评审稿已明确标识，待确认位置均引用问题编号。
- 正式稿的阻断问题已关闭，或受影响内容已经移出发布范围。

### 9.2 输出独立问题清单

问题清单是评审材料，默认不写进正式 PRD 正文。不得为了完成正文而替问题选择默认答案。

| 问题编号 | 处理状态 | 影响等级 | 关联位置 | 问题类型 | 问题描述 | 可能影响 | 改进建议 | 待确认事项 | 结论/关闭记录 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Q-01 | 阻断/非阻断 | 高/中/低 | {章节/字段/流程节点/CP-xx/R-xx} | 冲突/漏洞/歧义/缺失 | {具体问题} | {可能结果} | {建议补充或统一的规则} | {需要谁决定什么} | {结论、确认人和日期} |

没有问题时，明确写：`未发现待确认的冲突、漏洞、歧义或缺失。` 不得为了凑清单虚构问题。

## 10. 可直接使用的模板

### 10.1 普通 PRD 模板

```markdown
## {章节编号}. {业务对象 + 要解决的问题}

### {章节编号}.1 需求目的与范围

{说明现有场景、具体问题、需求目标或预期业务结果、纳入范围和排除范围。}

### {章节编号}.2 业务全貌

{说明参与方、核心对象、上游输入、主要环节和下游输出。达到第 5.2 节条件时放置 D2 主流程总览图；达到第 5.4 节条件时另放核心对象关系图，并补齐图下说明。}

### {章节编号}.3 成功主流程

{按业务发生顺序说明主要成功路径。}

### {章节编号}.4 分场景规则

#### R-01 {场景或规则标题}

{按第 6.2 节对应规则类型完整描述。达到第 6.8 节条件时放置 PlantUML，否则用正文步骤说明。}

### {章节编号}.5 业务落点与操作（按需）

{页面型需求写明产品或系统、入口、页面和业务区域，再列关键字段、操作和交互约束；业务工作项、后台自动作业、接口或集成事件按第 6.7 节说明落点与业务契约。}

### {章节编号}.6 字段、计算、状态与权限（按需）

{使用字段表、计算规则、状态矩阵或权限规则；环节正文通过 R-xx 引用，不重复定义。}

### {章节编号}.7 业务数据示例（按需）

{使用 EX-xx 并引用 R-xx；按实际存在的正常、关键分支和高风险边界提供示例。}

### {章节编号}.8 异常与边界

{说明失败、超时、重复、冲突、重试、回退、补偿、兼容和存量处理。}

### {章节编号}.9 结果与下游衔接

{说明输出结果、使用方和后续流程。}
```

目的、范围、业务全貌、实际存在的规则、异常边界和结果衔接是必需信息；其余内容按触发条件保留、合并或删除，不得删除实际存在的业务规则。

普通 PRD 未触发第 4.4 节的规则编号条件时，可以删除模板中的 `R-xx` 编号；使用图、数据示例或跨节规则引用时，按第 4.4 节补齐 `R/EX` 编号。逻辑问题始终使用 `Q-xx`，可以直接关联章节、字段或流程节点，不必为了问题编号额外创建 `R-xx`。

### 10.2 变更需求模板

```markdown
# 变更需求：{修改对象 + 修改动作}

## 1. 变更原因

{按“现有处理 → 触发场景 → 可观察问题或风险 → 业务影响 → 修改目的”说明。}

## 2. 变更基线

| 需求/变更编号 | 关联 PRD 及规则 | 关联原型 | 当前版本 | 基线日期与依据 |
| --- | --- | --- | --- | --- |
| {编号} | {文件、章节、R-xx 或链接} | {链接/版本} | {线上或已确认版本} | {日期、页面或资料} |

## 3. 全局变更范围

- 纳入：{本次修改的产品、系统、模块、用户和数据范围}
- 排除：{明确不在本次范围内的产品、系统、模块、用户和数据范围}

## 4. 变更总览

| 变更点 | 业务标题 | 变更落点摘要 | 修改前摘要 | 修改后摘要 | 分项规则链接 |
| --- | --- | --- | --- | --- | --- |
| CP-01 | {对象 + 动作} | {产品/系统 + 页面/工作项/后台作业/接口} | {当前结果} | {目标结果} | {章节链接} |

## 5. 变更落点

### 5.1 页面型变更（按需）

| 变更点 | 所属产品/系统 | 一级入口 | 二级入口 | 页面 | 业务区域 | 操作入口 |
| --- | --- | --- | --- | --- | --- | --- |
| CP-01 | {名称} | {菜单/导航} | {菜单/导航} | {页面} | {区域} | {按钮或入口} |

### 5.2 业务工作项型变更（按需）

| 变更点 | 所属产品/系统 | 工作项名称 | 创建入口 | 业务对象 | 处理队列/角色 | 分派/领取方式 | 处理页面 | 完成条件 | 转派/并发/超时 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CP-xx | {名称} | {名称} | {创建条件} | {对象} | {队列或角色} | {方式} | {页面} | {条件} | {处理规则} |

### 5.3 后台自动作业型变更（按需）

| 变更点 | 所属产品/系统 | 后台作业名称 | 触发方式/频率 | 处理对象与范围 | 数据来源 | 输出及写入位置 | 失败/重试 | 重复执行处理 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CP-xx | {名称} | {名称} | {时点或条件} | {对象与范围} | {来源} | {结果及位置} | {处理} | {防重} |

### 5.4 接口或集成事件型变更（按需）

| 变更点 | 提供方 | 调用方/消费方 | 接口/事件业务名称 | 触发条件 | 关键业务输入 | 业务输出 | 失败/超时结果 | 重复请求处理 | 兼容范围 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CP-xx | {名称} | {名称} | {业务名称} | {条件} | {字段/对象} | {结果} | {处理} | {幂等业务口径} | {版本或调用方} |

## 6. 变更后业务全貌（按需）

{变更改变跨环节流程或对象关系时，按第 5 章说明并绘图；局部简单调整删除本节。}

## 7. 分项变更内容

### CP-01 {变更点标题}

- 修改前表现：{基于已确认基线说明当前页面、业务工作项、后台自动作业、接口或数据结果}
- 修改后表现：{说明变更完成后的可观察结果}
- 系统调整对象：{页面、区域、按钮、业务工作项、后台自动作业、接口、字段、规则、状态、导入模板、导出文件或其他业务可感知产物；不写源代码文件}
- 本点特有边界：{只写该点邻接且未在全局范围定义的边界；无则删除}

#### R-01 {正式规则标题}

{按第 6.2 节的对应规则类型完整描述。}

#### 字段、计算、状态、权限、原型或异常（按需）

{使用第 6 章对应结构，并通过 R-xx 关联。}

#### 本变更点特有的生效与存量差异（按需）

{只写偏离全局策略的内容并引用对应 R-xx；各变更点独立生效时，在此完整使用第 6.9 节表格。}

#### 业务数据示例（按需）

| 示例编号 | 主要验证规则 | 其他关联规则 | 场景 | 前置条件与输入数据 | 操作或系统触发 | 预期结果 | 不应发生 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| EX-01 | R-01 | {R-xx 或不适用} | {正常/分支/边界} | {具体虚构数据} | {操作/触发} | {结果} | {风险或“不适用”} |

## 8. 全局生效与存量策略（按需）

| 关联变更点/规则 | 数据范围/状态 | 判断时点 | 是否采用新规则 | 处理方式 | 预期结果 | 重复执行/失败处理 |
| --- | --- | --- | --- | --- | --- | --- |
| CP-xx / R-xx | {新数据/未完成/已完成或锁定数据} | {条件} | 是/否 | {处理} | {结果} | {处理} |

{多个变更点没有共用策略、且已在各分项完整说明时，删除本节。}

## 9. 全局保持不变的规则（按需）

- {只列跨多个变更点且容易被误改的高风险规则，并引用原规则编号或位置。}

## 10. 结果与上下游影响

{说明本次变更的最终输出、下游使用方、外部契约和兼容范围。}
```

根据变更类型保留对应落点表，不在页面表中填满“不适用”。同一内容不得同时出现在全局排除项、变更点边界和全局保持不变规则中。

## 11. 提交前检查清单

### 11.1 路线、目的与结构

- 是否选择了普通 PRD 或变更需求路线，并使用对应骨架？
- 改写既有普通 PRD 是否建立可追溯基线，并保留审查依据？
- 是否用简洁语义说明缘由、具体问题和目标结果？
- 是否写清纳入范围、排除范围、参与方、核心对象、输入和输出？
- 是否先总后分、由浅入深，且相邻小节有明确衔接？
- 每条规则是否只有一个完整主描述位置？

### 11.2 流程、对象和图

- 是否按绘图条件决定使用 D2，而不是按对象或步骤数量机械加图？
- 关键正常分支改变后续环节时，主流程总览是否展示分流与汇合？
- 对业务结果有影响的系统自动作业是否保留，接口调用等实现步骤是否排除？
- 静态对象图是否使用业务分组或容器，并只表达归属、引用和数量关系？
- 是否先确认规则；每个 D2 环节是否有可定位的正文展开；复杂环节是否按第 6.8 节条件绘制 PlantUML？
- 图下说明是否完整，图与正文是否使用同一名称、条件和结果？
- 变更流程图是否保留一个上游入口、完整变化路径和一个下游出口，并压缩未变化链路？

### 11.3 规则、字段、页面和数据示例

- 各类规则是否使用第 6.2 节对应结构，而非全部硬套四要素？
- 多条件组合是否拆分，必要时是否使用决策表并给出兜底结果？
- 页面、业务工作项、后台自动作业、接口或集成事件是否按第 6.7 节写清业务落点？
- 新增、修改、参与判断或构成数据契约的字段是否逐项列出？
- 字段达到六个或跨对象时是否按业务对象或用途分组？
- 数值、单位、精度、舍入、时间边界、状态和权限是否按需补齐？
- 原型概述是否只保留业务区域、关键字段、关键操作和关键交互约束？
- 数据示例是否引用正式规则，按实际存在的正常、分支和边界场景提供？
- “不应发生”是否只在存在明确风险时填写，否则标记“不适用”？

### 11.4 变更需求专项

- 变更需求是否有可追溯基线，标题是否写明修改对象和动作？
- `CP/R/EX/Q` 编号是否按适用范围使用、在文档内全局唯一，并能串联总览、落点、规则、示例和问题？
- 变更点是否按独立业务结果、落点、上线/回滚或存量策略合理拆分？
- 页面型变更是否定位到产品或系统、菜单、页面、区域和按钮？
- 业务工作项是否写明创建、处理对象、队列或角色、分派/领取、状态、完成、转派、并发和超时？
- 后台自动作业是否写明触发、范围、来源、输出位置、失败和重复执行？
- 接口或集成事件是否写明参与方、输入输出、失败、重复请求和兼容范围？
- 每个变更点是否写清修改前、修改后、系统调整对象和局部边界？
- 生效时点、新数据、未完成数据、已完成或锁定数据是否分别有处理规则？
- 共用生效策略是否只在全局完整说明，各变更点是否只写差异或引用？
- 全局范围、局部边界和保持不变规则是否没有重复或冲突？

### 11.5 措辞、逻辑与交付

- 是否解释生僻名词和内部简称，并删除非需求、常识、重复和歧义表达？
- 是否仍存在“相关”“适当”“按实际情况”“保持一致”等无法执行的词？
- 正文、图、原型、表格、示例和问题清单是否同一口径？
- 是否完成冲突、漏洞、歧义和缺失检查，并独立输出问题清单？
- 是否把处理状态和影响等级分开判断？
- 正式稿是否已关闭所有阻断问题，或将受影响规则移出发布范围？
- 正文是否已经可以直接作为实现和检查依据，且没有复制验收标准？

D2 和 PlantUML 的语法、渲染及布局不属于常规提交检查。只有审阅发现异常或产品经理明确要求时才执行。
