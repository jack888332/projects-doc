# BMS ARAP 代收代付费项拆分技术调整方案与计划

## 1. 背景

当前 BMS 费项类型中存在 `ARAP`，用于笼统表示“代收代付”。按 `612baf870c4f4a957ccf03b570905ae4a1c0abdc` 提交补充后的 PRD 口径，代收资金与代付资金在应收账单中的金额方向不同：

1. `应收扣减类`：应收账单记为负数。
2. `代收类`：应收账单记为负数，用于冲减客户应付金额。
3. `代付类`：应收账单记为正数，表示客户应补偿我方垫付或支付的金额。
4. `非费项`：不纳入任何账单核销范围，仅在返款账单、回款管理等特定位置展示或核对。

因此，`ARAP` 需要拆成两个明确类型，避免同一类型下既有负向资金又有正向资金，导致应收账单生成、汇总、核销和前端筛选口径不一致。

## 2. 指定提交口径摘要

提交 `612baf870c4f4a957ccf03b570905ae4a1c0abdc` 对 PRD 的关键补充如下：

| 业务类型 | 业务含义 | 应收账单金额方向 |
| :--- | :--- | :--- |
| 应收类 | 客户应支付给我方的服务费、运费、附加费等 | 正数 |
| 应收扣减类 | 优惠、赔付等冲减客户应付金额 | 负数 |
| 应付类 | 我方对外成本或供应商侧应付 | 不进入客户应收 |
| 代收类 | 我方代客户收取的资金 | 负数 |
| 代付类 | 我方代客户垫付或支付的资金 | 正数 |
| 非费项 | 事实金额、核对金额、展示金额 | 不进入核销汇总 |

PRD 中与本次拆分直接相关的来源映射如下：

| 来源字段或来源条件 | 费项名称 | 新费用类型 |
| :--- | :--- | :--- |
| `ofp_ofdb1.sale_order_header.collection_price` | 代收货款 | 代收类 |
| `ofp_ofdb1.sale_order_header.advance_amount` | 垫付金额 | 代付类 |
| `ofp_ofdb1.sale_order_fee_detail.collection` | 应返货款 | 代收类 |
| `ofp_ofdb1.sale_order_additional_matter where fee_item_type="代客收台币"` | 代客收台币 | 代收类 |
| `ofp_ofdb1.sale_order_additional_matter where fee_item_type="代客付台币"` | 代客付台币 | 代付类 |
| `ofp_ofdb1.sale_order_header.cod_price` | 到付金额 / COD金额 | 非费项 |
| `ofp_ofdb1.sale_order_header.cod_amount` | 货到付款手续费 | 非费项 |
| `ofp_ofdb1.sale_order_fee_detail.recovery_money` | 实收回款 | 非费项 |

## 3. 当前代码与数据现状

### 3.1 代码现状

1. `BmsConstants` 当前定义了 `AR`、`ARD`、`AP`、`ARAP`、`NON_FEE`，其中 `ARAP` 未拆分代收/代付。
2. `BillGenerateServiceImpl#signedAmount` 当前仅对 `ARD` 取负，对 `NON_FEE` 返回 0，其他类型按正数处理。
3. 应收账单生成过程中，部分内存汇总调用了 `signedAmount`，但 `fee_detail` 落库金额仍可能沿用来源原始正数，后续 `ar_bill_currency_summary` 又从 `fee_detail.amount_bill_currency` 汇总，存在“内存计算方向”和“落库汇总方向”不一致风险。
4. `BillGenerateMapper` 中汇总口径排除了 `NON_FEE`，但没有区分 `AR_COLLECT` 和 `AR_PAY`。
5. 前端 `admin_shell/src/views/billing/feeItem/index.vue` 当前仍展示 `ARAP: 代收代付`。
6. `FeeIndex`、`FeeDetail`、`FeeDetailDTO`、`FeeDetailSaveDTO` 等注释仍是旧口径。

### 3.2 当前数据库样例

按现有测试库观察，`business_type_fee_index` 中仍存在以下 `ARAP` 配置：

| 业务场景 | 费项编码 | 费项名称 | 当前类型 | 调整后类型 |
| :--- | :--- | :--- | :--- | :--- |
| `PEER` / `CONSOLIDATION` | `FEE0022` | 代收货款 | `ARAP` | `AR_COLLECT` |
| `PEER` / `CONSOLIDATION` | `FEE0026` | 垫付金额 | `ARAP` | `AR_PAY` |
| `PEER` / `CONSOLIDATION` | `FEE0040` | 代客收台币 | `ARAP` | `AR_COLLECT` |
| `PEER` / `CONSOLIDATION` | `FEE0041` | 代客付台币 | `ARAP` | `AR_PAY` |
| `COD_ORDER` | `FEE0050` | 代收货款 | `ARAP` | `AR_COLLECT` |

## 4. 目标口径

### 4.1 新增费用类型

| 类型编码 | 展示名称 | 说明 |
| :--- | :--- | :--- |
| `AR_COLLECT` | 代收类 | 代客户收取的资金，在应收账单按负数处理 |
| `AR_PAY` | 代付类 | 代客户垫付或支付的资金，在应收账单按正数处理 |

`ARAP` 作为历史兼容值保留识别能力，但不允许新增配置继续选择。数据迁移完成后，配置主档和业务场景绑定表不应再存在启用状态的 `ARAP`。

### 4.2 应收账单金额方向

| 费用类型 | 落库金额方向 | 汇总金额方向 |
| :--- | :--- | :--- |
| `AR` | 正数 | 正数 |
| `ARD` | 负数 | 负数 |
| `AR_COLLECT` | 负数 | 负数 |
| `AR_PAY` | 正数 | 正数 |
| `AP` | 不进入客户应收账单 | 不进入客户应收账单 |
| `NON_FEE` | 不进入可核销明细；如需展示，使用非核销展示明细 | 不汇总 |
| `ARAP` | 仅历史兼容，按费项编码/名称兜底判断 | 不新增 |

建议在账单生成写入 `fee_detail` 前统一规范金额方向，确保 `amount_fee_currency`、`amount_bill_currency`、`amount_fin_currency` 三个金额字段方向一致。后续汇总 SQL 直接按已规范的 `fee_detail` 金额求和，避免同一金额在不同环节重复取反。

## 5. 技术调整方案

### 5.1 后端常量与模型

1. 在 `BmsConstants` 新增：
   - `FEE_TYPE_AR_COLLECT = "AR_COLLECT"`
   - `FEE_TYPE_AR_PAY = "AR_PAY"`
2. 保留 `FEE_TYPE_ARAP = "ARAP"`，注释标记为历史兼容类型。
3. 更新 `FeeIndex`、`FeeDetail`、`FeeDetailDTO`、`FeeDetailSaveDTO` 的费用类型注释。
4. 如存在费用类型枚举或接口返回字典，补充 `AR_COLLECT`、`AR_PAY`，隐藏或禁用 `ARAP` 新增入口。

### 5.2 账单生成

1. 在 `BillGenerateServiceImpl` 中新增统一金额方向方法，例如 `normalizeAmountDirection(rule, amount)`。
2. 规则如下：
   - `ARD`、`AR_COLLECT`：统一取绝对值后转负数。
   - `AR`、`AR_PAY`：统一取绝对值后转正数。
   - `NON_FEE`：不进入应收汇总；如当前链路仍需展示，必须明确标记为非核销展示。
   - `ARAP`：仅作为历史兼容，按 `feeCode` / `feeName` 判断：
     - `FEE0022`、`FEE0050`、`FEE0040`、`代收货款`、`代客收台币`：按 `AR_COLLECT`。
     - `FEE0026`、`FEE0041`、`垫付金额`、`代客付台币`：按 `AR_PAY`。
3. 在构建 `fee_detail` 落库对象时即完成方向规范，不只在内存汇总处处理。
4. `ar_bill_currency_summary`、账单主表金额、未收金额均基于已规范后的 `fee_detail` 金额计算。
5. 账单重算时：
   - 草稿中、待审核账单可重算并按新口径重写明细。
   - 待结清账单如需重算，应先折返到待审核后再重算。
   - 已结清账单不得重算，只能通过调账中心冲正。

### 5.3 数据访问层

1. 检查 `BillGenerateMapper` 中所有应收金额汇总 SQL，确保排除 `NON_FEE`，并不再对 `AR_COLLECT` / `AR_PAY` 额外重复取反。
2. 若存在直接从来源数据临时表或 Map 汇总金额的 SQL，需要同步调用统一金额方向逻辑或迁移到 Java 层规范后再入库。
3. 保留历史 `ARAP` 明细的查询展示能力，但新生成明细应落入新类型。

### 5.4 前端

1. `admin_shell` 费项管理页面费用类型字典调整：
   - 新增 `AR_COLLECT: 代收类`
   - 新增 `AR_PAY: 代付类`
   - 旧 `ARAP: 代收代付` 不再作为新增/编辑可选项；如果历史数据存在，只做只读展示。
2. 筛选区支持按新类型筛选。
3. 列表展示、详情展示、导出字段同步展示新类型名称。

### 5.5 数据归档

涉及表字段注释调整时，需要同步更新 `aidocs/technical-caliber/sql/ar_bill.sql` 中以下表的完整结构：

1. `fee_index`
2. `business_type_fee_index`
3. `fee_detail`

## 6. 最终调整 SQL

> 说明：以下 SQL 按“可重复执行、先查后改”的思路编写。执行前建议先在目标环境备份 `fee_index`、`business_type_fee_index`、`fee_detail` 三张表，并先执行核对查询确认影响范围。

```sql
-- =========================================================
-- BMS ARAP 代收代付拆分调整 SQL
-- 日期：2026-07-09
-- 口径：ARAP 拆分为 AR_COLLECT（代收类）与 AR_PAY（代付类）
-- =========================================================

-- 1. 执行前核对：当前仍为 ARAP 的费项主档和场景绑定
SELECT id, fee_code, fee_name, fee_type, enabled
FROM fee_index
WHERE fee_type = 'ARAP'
ORDER BY id;

SELECT id, business_type_code, business_type_name, fee_code, fee_name, fee_type, enabled
FROM business_type_fee_index
WHERE fee_type = 'ARAP'
ORDER BY business_type_code, fee_code, id;

-- 2. 表字段注释调整：保留字段宽度，补充新枚举说明
ALTER TABLE fee_index
    MODIFY COLUMN fee_type VARCHAR(16) NOT NULL
    COMMENT '费用类型：AR应收，ARD应收扣减，AP成本，AR_COLLECT代收，AR_PAY代付，ARAP历史代收代付，NON_FEE非费项';

ALTER TABLE business_type_fee_index
    MODIFY COLUMN fee_type VARCHAR(16) NOT NULL
    COMMENT '费用类型：AR应收，ARD应收扣减，AP成本，AR_COLLECT代收，AR_PAY代付，ARAP历史代收代付，NON_FEE非费项';

ALTER TABLE fee_detail
    MODIFY COLUMN fee_type VARCHAR(16) DEFAULT NULL
    COMMENT '费用类型：AR应收，ARD应收扣减，AP成本，AR_COLLECT代收，AR_PAY代付，ARAP历史代收代付，NON_FEE非费项';

-- 3. 费项主档：ARAP 拆分为代收类
UPDATE fee_index
SET fee_type = 'AR_COLLECT',
    updated_at = NOW(),
    updated_by = 'system_arap_split_20260709'
WHERE is_deleted = 0
  AND (
        fee_code IN ('FEE0022', 'FEE0050', 'FEE0040')
        OR fee_name IN ('代收货款', '应返货款', '代客收台币')
      );

-- 4. 费项主档：ARAP 拆分为代付类
UPDATE fee_index
SET fee_type = 'AR_PAY',
    updated_at = NOW(),
    updated_by = 'system_arap_split_20260709'
WHERE is_deleted = 0
  AND (
        fee_code IN ('FEE0026', 'FEE0041')
        OR fee_name IN ('垫付金额', '代客付台币')
      );

-- 5. 场景绑定：ARAP 拆分为代收类
UPDATE business_type_fee_index
SET fee_type = 'AR_COLLECT',
    updated_at = NOW(),
    updated_by = 'system_arap_split_20260709'
WHERE is_deleted = 0
  AND (
        fee_code IN ('FEE0022', 'FEE0050', 'FEE0040')
        OR fee_name IN ('代收货款', '应返货款', '代客收台币')
      );

-- 6. 场景绑定：ARAP 拆分为代付类
UPDATE business_type_fee_index
SET fee_type = 'AR_PAY',
    updated_at = NOW(),
    updated_by = 'system_arap_split_20260709'
WHERE is_deleted = 0
  AND (
        fee_code IN ('FEE0026', 'FEE0041')
        OR fee_name IN ('垫付金额', '代客付台币')
      );

-- 7. 按 PRD 口径补正非费项类型
UPDATE fee_index
SET fee_type = 'NON_FEE',
    updated_at = NOW(),
    updated_by = 'system_arap_split_20260709'
WHERE is_deleted = 0
  AND (
        fee_code IN ('FEE0024', 'FEE0025')
        OR fee_name IN ('到付金额', 'COD金额', '货到付款手续费', '实收回款', '实回货款')
      );

UPDATE business_type_fee_index
SET fee_type = 'NON_FEE',
    updated_at = NOW(),
    updated_by = 'system_arap_split_20260709'
WHERE is_deleted = 0
  AND (
        fee_code IN ('FEE0024', 'FEE0025')
        OR fee_name IN ('到付金额', 'COD金额', '货到付款手续费', '实收回款', '实回货款')
      );

-- 8. 按 PRD 口径补正应收扣减类
UPDATE fee_index
SET fee_type = 'ARD',
    updated_at = NOW(),
    updated_by = 'system_arap_split_20260709'
WHERE is_deleted = 0
  AND (
        fee_code IN ('FEE0007', 'FEE0011', 'FEE0015', 'FEE_CLAIM_COMPENSATION')
        OR fee_name IN ('满减活动优惠金额', '优惠券优惠金额', '积分优惠金额', '理赔赔付', '理赔费')
      );

UPDATE business_type_fee_index
SET fee_type = 'ARD',
    updated_at = NOW(),
    updated_by = 'system_arap_split_20260709'
WHERE is_deleted = 0
  AND (
        fee_code IN ('FEE0007', 'FEE0011', 'FEE0015', 'FEE_CLAIM_COMPENSATION')
        OR fee_name IN ('满减活动优惠金额', '优惠券优惠金额', '积分优惠金额', '理赔赔付', '理赔费')
      );

-- 9. 补充缺失费项主档：代客收台币
INSERT INTO fee_index (
    fee_code, fee_name, fee_type, attachment_object,
    legacy_data_source, scenario_tag, applicable_order_source,
    enabled, remark, is_deleted, created_at, created_by, updated_at, updated_by
)
SELECT
    'FEE0040', '代客收台币', 'AR_COLLECT', 'ORDER',
    'ofp_ofdb1.sale_order_additional_matter.fee_amount', '附加费', '不限',
    1, 'ARAP拆分补充：代收类', 0, NOW(), 'system_arap_split_20260709', NOW(), 'system_arap_split_20260709'
WHERE NOT EXISTS (
    SELECT 1 FROM fee_index WHERE is_deleted = 0 AND fee_code = 'FEE0040'
);

-- 10. 补充缺失费项主档：代客付台币
INSERT INTO fee_index (
    fee_code, fee_name, fee_type, attachment_object,
    legacy_data_source, scenario_tag, applicable_order_source,
    enabled, remark, is_deleted, created_at, created_by, updated_at, updated_by
)
SELECT
    'FEE0041', '代客付台币', 'AR_PAY', 'ORDER',
    'ofp_ofdb1.sale_order_additional_matter.fee_amount', '附加费', '不限',
    1, 'ARAP拆分补充：代付类', 0, NOW(), 'system_arap_split_20260709', NOW(), 'system_arap_split_20260709'
WHERE NOT EXISTS (
    SELECT 1 FROM fee_index WHERE is_deleted = 0 AND fee_code = 'FEE0041'
);

-- 11. 补充缺失费项主档：COD 代收货款
INSERT INTO fee_index (
    fee_code, fee_name, fee_type, attachment_object,
    legacy_data_source, scenario_tag, applicable_order_source,
    enabled, remark, is_deleted, created_at, created_by, updated_at, updated_by
)
SELECT
    'FEE0050', '代收货款', 'AR_COLLECT', 'ORDER',
    'ofp_ofdb1.sale_order_header.collection_price', 'COD返款', 'COD_ORDER',
    1, 'ARAP拆分补充：COD订单代收货款', 0, NOW(), 'system_arap_split_20260709', NOW(), 'system_arap_split_20260709'
WHERE NOT EXISTS (
    SELECT 1 FROM fee_index WHERE is_deleted = 0 AND fee_code = 'FEE0050'
);

-- 12. 补充业务场景绑定：仅当对应来源规则已存在时补充，避免猜测来源规则
INSERT INTO business_type_fee_index (
    business_type_code, business_type_name,
    fee_index_id, fee_code, fee_name, fee_type,
    fee_source_rule_id, priority, enabled, remark,
    is_deleted, created_at, created_by, updated_at, updated_by
)
SELECT
    bt.business_type_code,
    bt.business_type_name,
    fi.id,
    fi.fee_code,
    fi.fee_name,
    fi.fee_type,
    fsr.id,
    100,
    1,
    'ARAP拆分补充：代客收台币',
    0,
    NOW(),
    'system_arap_split_20260709',
    NOW(),
    'system_arap_split_20260709'
FROM (
    SELECT 'PEER' AS business_type_code, '同行订单' AS business_type_name
    UNION ALL
    SELECT 'CONSOLIDATION', '集运订单'
) bt
JOIN fee_index fi ON fi.is_deleted = 0 AND fi.fee_code = 'FEE0040'
JOIN fee_source_rule fsr ON fsr.is_deleted = 0
    AND fsr.enabled = 1
    AND fsr.source_table LIKE '%sale_order_additional_matter%'
    AND fsr.source_amount_column = 'fee_amount'
    AND fsr.filter_params_json LIKE '%代客收台币%'
WHERE NOT EXISTS (
    SELECT 1
    FROM business_type_fee_index bfi
    WHERE bfi.is_deleted = 0
      AND bfi.business_type_code = bt.business_type_code
      AND bfi.fee_code = 'FEE0040'
);

INSERT INTO business_type_fee_index (
    business_type_code, business_type_name,
    fee_index_id, fee_code, fee_name, fee_type,
    fee_source_rule_id, priority, enabled, remark,
    is_deleted, created_at, created_by, updated_at, updated_by
)
SELECT
    bt.business_type_code,
    bt.business_type_name,
    fi.id,
    fi.fee_code,
    fi.fee_name,
    fi.fee_type,
    fsr.id,
    100,
    1,
    'ARAP拆分补充：代客付台币',
    0,
    NOW(),
    'system_arap_split_20260709',
    NOW(),
    'system_arap_split_20260709'
FROM (
    SELECT 'PEER' AS business_type_code, '同行订单' AS business_type_name
    UNION ALL
    SELECT 'CONSOLIDATION', '集运订单'
) bt
JOIN fee_index fi ON fi.is_deleted = 0 AND fi.fee_code = 'FEE0041'
JOIN fee_source_rule fsr ON fsr.is_deleted = 0
    AND fsr.enabled = 1
    AND fsr.source_table LIKE '%sale_order_additional_matter%'
    AND fsr.source_amount_column = 'fee_amount'
    AND fsr.filter_params_json LIKE '%代客付台币%'
WHERE NOT EXISTS (
    SELECT 1
    FROM business_type_fee_index bfi
    WHERE bfi.is_deleted = 0
      AND bfi.business_type_code = bt.business_type_code
      AND bfi.fee_code = 'FEE0041'
);

-- 13. 执行后核对：正常情况下，启用配置不应再有 ARAP
SELECT id, fee_code, fee_name, fee_type, enabled
FROM fee_index
WHERE fee_type = 'ARAP'
ORDER BY id;

SELECT id, business_type_code, business_type_name, fee_code, fee_name, fee_type, enabled
FROM business_type_fee_index
WHERE fee_type = 'ARAP'
ORDER BY business_type_code, fee_code, id;

-- 14. 执行后核对：本次核心费项类型
SELECT fee_code, fee_name, fee_type, enabled
FROM fee_index
WHERE fee_code IN ('FEE0022', 'FEE0026', 'FEE0040', 'FEE0041', 'FEE0050', 'FEE0024', 'FEE0025')
   OR fee_name IN ('代收货款', '垫付金额', '代客收台币', '代客付台币', '到付金额', 'COD金额', '货到付款手续费', '实收回款', '实回货款')
ORDER BY fee_code, id;

SELECT business_type_code, fee_code, fee_name, fee_type, enabled
FROM business_type_fee_index
WHERE fee_code IN ('FEE0022', 'FEE0026', 'FEE0040', 'FEE0041', 'FEE0050', 'FEE0024', 'FEE0025')
   OR fee_name IN ('代收货款', '垫付金额', '代客收台币', '代客付台币', '到付金额', 'COD金额', '货到付款手续费', '实收回款', '实回货款')
ORDER BY business_type_code, fee_code, id;
```

## 7. 调整计划

| 阶段 | 工作项 | 输出 |
| :--- | :--- | :--- |
| 1 | 确认新类型编码、PRD 冲突点、历史账单处理边界 | 确认后的最终口径 |
| 2 | 调整基础数据 SQL，并在测试库执行核对查询 | 可执行 SQL 与影响行数 |
| 3 | 后端新增常量与金额方向统一方法 | `AR_COLLECT` / `AR_PAY` 生成方向正确 |
| 4 | 调整账单生成落库金额方向和汇总口径 | `fee_detail`、币种汇总、账单主表金额一致 |
| 5 | 调整前端费项类型字典和筛选展示 | 页面不再新增 `ARAP` |
| 6 | 同步表结构归档和技术文档 | `ar_bill.sql` 与代码一致 |
| 7 | 用指定任务和人工账单回归 | 代收类负数、代付类正数、非费项不核销 |

## 8. 验证方案

1. 基础数据验证：
   - `fee_index`、`business_type_fee_index` 中启用配置不再存在 `ARAP`。
   - `FEE0022`、`FEE0050`、`FEE0040` 为 `AR_COLLECT`。
   - `FEE0026`、`FEE0041` 为 `AR_PAY`。
2. 账单生成验证：
   - 含代收货款的应收账单，代收货款明细金额为负数。
   - 含垫付金额的应收账单，垫付金额明细金额为正数。
   - 含代客收台币的应收账单，代客收台币明细金额为负数。
   - 含代客付台币的应收账单，代客付台币明细金额为正数。
   - 账单主表金额、币种汇总金额、详情页金额、导出金额一致。
3. 非费项验证：
   - 到付金额、货到付款手续费、实收回款不进入应收账单核销汇总。
   - 如页面需要展示，展示入口与可核销明细有明确区分。
4. 兼容性验证：
   - 历史 `ARAP` 明细可查询展示。
   - 旧配置快照中如仍存在 `ARAP`，生成逻辑能按费项编码兜底处理方向。

## 9. 疑问点

1. `sale_order_fee_detail.collection` 在 PRD 表格中标为“代收类”，但说明中又写“应返货款属于代付类资金”，这两处口径不一致，需要确认最终按 `AR_COLLECT` 还是 `AR_PAY`。
2. 新类型编码建议为 `AR_COLLECT` / `AR_PAY`，是否确认采用这两个编码？如果产品或数据字典已有固定编码，需要以数据字典为准。
3. 历史已生成且未结清的 `fee_detail.fee_type = ARAP` 明细是否需要批量迁移并重算金额方向？建议只允许草稿中、待审核账单重算；待结清需折返待审核；已结清不改历史，通过调账中心处理。
4. `NON_FEE` 当前代码存在“写入明细但排除汇总”的实现痕迹，而 PRD 新口径是“不写入任何账单的可核销明细”。是否允许保留一个非核销展示明细，还是账单明细表完全不落库？
5. `FEE0022` 与 `FEE0050` 都是“代收货款”，分别出现在普通集运/同行和 COD 场景，是否统一按 `AR_COLLECT` 处理？
6. `collection_premium_amount` / `receivable_collection_amount` 是代收货款手续费，PRD 口径仍为应收类；若返款配置中选择直接扣减，应由返款配置规则决定排除应收账单，而不应改成代收类。该理解是否确认？
7. 如果目标库缺少 `代客收台币` / `代客付台币` 对应的 `fee_source_rule`，本方案 SQL 只补充费项主档，不猜测创建来源规则。是否需要另行补充来源规则初始化 SQL？
8. 是否需要对前端“费用类型”筛选保留 `ARAP` 作为历史筛选项？建议保留只读筛选，新增/编辑下拉不展示。
