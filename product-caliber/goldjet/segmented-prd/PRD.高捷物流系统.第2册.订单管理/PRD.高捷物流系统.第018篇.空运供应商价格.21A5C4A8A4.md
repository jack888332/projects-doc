# 第018篇

> 本篇定位：空运供应商价格；主要内容：供应商价格、梯度与附加费、客户应收计费；文档角色：模块档；文档ID：21A5C4A8A4

**主流程入口：** 本篇关联[前置准备与服务开通流程](../PRD.高捷物流系统.第1册.需求框架/PRD.高捷物流系统.第004篇.前置准备与服务开通流程.852BDA78EA.md#doc-852BDA78EA-service-readiness)、[税金管理与财务结算流程](../PRD.高捷物流系统.第1册.需求框架/PRD.高捷物流系统.第009篇.税金管理与财务结算流程.077CC05853.md#doc-077CC05853-tax-settlement)。
<a id="doc-21A5C4A8A4-section-240243021622"></a>
## 1. 空运供应商价格

**业务入口：** 供应商价格

**概念模型：** [数据字典 · 合作方与报价实体](../PRD.高捷物流系统.第6册.运营支撑/PRD.高捷物流系统.第061篇.数据字典.7A3D9E1F50.md#doc-7A3D9E1F50-domain-partners-quotes)

<a id="doc-21A5C4A8A4-section-b036d60ab7df"></a>
### 1.1 供应商价格维护功能与规则

<a id="doc-21A5C4A8A4-section-d2de89f28aab"></a>
#### 1.1.1 供应商价格业务范围

本篇定义供应商价格的查询、新增、编辑、启用、失效及计费规则，并定义客户应收费用的计算口径。

<a id="doc-21A5C4A8A4-section-9210aa5d4506"></a>
#### 1.1.2 供应商价格查询与操作

**界面原型概述 · 供应商价格查询栏**

- **区域字段或业务元素**：成本类型、状态、计费单位、服务类型、供应商。
- **区域级通用规则**：各条件从候选项中选择。

| 字段或控件特例 | 适用条件 | 特例约束或业务结果 |
| --- | --- | --- |
| 成本类型 | 查询价格时 | 取费用项，支持模糊查询。 |
| 状态 | 查询价格时 | 可选“已生效”“失效”。 |
| 计费单位 | 查询价格时 | 可选分单数（个）、主单数（个）、提单计费重（kg）、提单毛重（kg）、提单毛重/提单件数（kg/件）、提单件数。 |
| 服务类型 | 查询价格时 | 可选订舱、提货、仓储、中转、预配发送、报关、货站安检、清关派送八类服务。 |
| 供应商 | 查询价格时 | 取供应商档案，支持模糊查询。 |

**操作规则**

- 查询：未设置查询条件时查询全部记录；设置条件后展示匹配记录。查询出的数据按照“更新时间”倒序排列。

- 重置：重置查询条件。

- 新增：只有客服主管能看到该按钮。点击后，在价格维护列表上方新增一行输入项。

- 编辑：只有客服主管能看到该按钮。点击后，被编辑的价格数据显示在价格维护列表上方。

- 启用：只有客服主管能看到该按钮；点击后弹出“供应商价格规则启用”弹窗。

- 失效：只有客服主管能看到该按钮；点击后弹出“供应商价格规则失效”弹窗。

- 查看：当计费方式为非“常规”时，计费单价列不显示数据，显示“查看”按钮。点击“查看”按钮，出现计费方式详细弹窗。

**字段定义**

**界面原型概述 · 供应商价格维护**

- **区域字段或业务元素**：供应商、服务类型、成本类型、计费单位、计费方式、计费单价、税率、币种、创建人、生效日期、截止日期、更新时间、合同编号、状态。

| 字段或控件特例 | 特例约束或业务结果 |
| --- | --- |
| 供应商 | 候选项选择；选择供应商，支持模糊查询；允许新增供应商；必填；无预设值；可编辑。 |
| 服务类型 | 选项：订舱、提货、中转、仓储、预配发送、报关、货站安检、清关派送；支持模糊查询；必填；无预设值；可编辑。 |
| 成本类型 | 候选项选择；成本类型取自结算中心的费用项。支持模糊查询；必填；无预设值；可编辑。 |
| 计费单位 | 候选项选择；计费单位有以下几种：分单数(个)，主单数(个)，提单计费重(kg)，提单毛重(kg)，按提单件数(件)，提单件数/提单毛重；必填；无预设值；可编辑。 |
| 计费方式 | 候选项选择；计费方式有以下几种：常规方式、航晟运输报价、梯度报价、首加续报价；必填；无预设值；可编辑。 |
| 计费单价 | 纯数值，精确到小数点后2位；当计费方式为航晟运输报价、梯度报价、首加续报价时，计费单价为空，显示“查看”按钮；必填；无预设值；可编辑。 |
| 税率 | 整数；必填；默认值：0；可编辑。 |
| 币种 | 必填；无预设值；可编辑。 |
| 创建人 | 自动获取账户名；只读。 |
| 生效日期、截止日期 | 按日期范围选择，格式为Y-M-D；必填；无预设值；可编辑。 |
| 更新时间 | 按日期范围选择，格式为Y-M-D HH：MM；系统自动记录；只读。 |
| 合同编号 | 选填；无预设值；可编辑。 |
| 状态 | 已生效、失效；状态流转见[启用规则](#doc-21A5C4A8A4-section-05bc00aca127)和[失效及超期规则](#doc-21A5C4A8A4-section-3b13a22cc310)；只读。 |

**供应商费用结算规则**

| 服务类型 | 计费类型 | 单价 | 单位 | 计算公式 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 订舱 | 提单计费重 | 运费成本 | kg | 提单计费重*运费成本 | 仅适用于与航空公司签订合同的舱位 |
| 提货 | —— | —— | —— | —— | 航晟返还数据 |
| 中转 | —— | —— | —— | —— | 航晟返还数据 |
| 报关 | 分单数 | 150 | 张 | 分单数*150 | 允许手工修改 |
| 仓储 | 提单计费重 | 0.1 | kg | 提单计费重*0.1 |  |
| 预配发送 | 分单数 | 36 | 张 | 分单数*36 |  |
| 清关派送 | —— | —— | —— | —— | 手工录入 |
| 货站安检 | 提单计费重 | 0.1 | kg | 提单计费重*0.1 |  |

**客户费用结算规则**

| 服务类型 | 计费类型 | 单价 | 单位 | 计算公式 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 订舱 | 提单计费重 | 运费卖价 | kg | 提单计费重*运费卖价 | 运费卖价在客服创建订单时录入。 |
| 提货 | —— | —— | —— | —— | 客服根据航晟返回的应付数据加价，并手工录入报价 |
| 中转 | —— | —— | —— | —— |  |
| 报关 | —— | —— | —— | —— | 由客服手工录入对外报价 |
| 仓储 | 提单计费重 | 0.1 | kg | 提单计费重*0.1 | 与应付一致。 |
| 预配发送 | 分单数 | 50 | 张 | 分单数*50 |  |
| 清关派送 | —— | —— | —— | —— | 手工录入 |
| 货站安检 | 提单计费重 | 0.1 | kg | 提单计费重*0.1 | 与应付一致。 |

<a id="doc-21A5C4A8A4-section-8d5aa17bb0f4"></a>
#### 1.1.3 新增供应商价格规则

在“供应商价格维护列表页面”点击“新增”按钮，在供应商价格维护列表之上新增一行可编辑的价格规则。新增时，填写供应商、服务类型、成本类型、计费单位、计费方式、计费单价、税率、币种、生效日期、截止日期、合同编号。

**操作规则**

- 查询：在不选择任何查询字段下，点击“查询”按钮，默认所有查询条件。选择对应的查询条件，则显示符合结果的数据，查询出的数据按照“更新时间”倒序排列。查询时保留新增列表相关内容。

- 重置：重置查询条件。

- 保存：保存成功时提示“新增供应商价格规则成功”，并刷新页面。

- 取消：取消新增报价。

- 编辑：若计费方式选择为航晟运输报价、梯度报价、首加续报价，计费单价列显示“修改”按钮；点击“修改”，出现相对应的“新增计费方式”弹窗。

- 修改：点击“修改”按钮，弹出相对应的计费方式价格维护新增弹窗。

- 失效：在新增时，“失效”按钮置灰。

- 启用：在新增时，“启用”按钮置灰。

**保存校验规则**

同一个供应商、同一个服务类型、同一个成本类型、同一时间只支持维护一条价格维护数据。当新增的价格维护数据与之前的数据发生有效期重叠时，不允许新增。

<a id="doc-21A5C4A8A4-section-16b1f15d47b7"></a>
#### 1.1.4 编辑供应商价格规则

在“供应商价格维护列表页面”选择要编辑的一条价格维护数据，点击“编辑”按钮，在供应商价格维护列表之上编辑选中的价格维护数据。可编辑供应商、服务类型、成本类型、计费单位、计费方式、计费单价、税率、币种、生效日期、截止日期、合同编号。

**操作规则**

- 保存：保存成功时提示“编辑供应商价格规则成功”，并刷新页面。

- 取消：取消编辑价格。

- 新增：在编辑页面点击“新增”弹出提示弹窗：“有修改的内容尚未保存，是否新增”。

- 修改：若计费方式为航晟运输报价、梯度报价、首加续报价，计费单价列显示“修改”按钮；点击“修改”，出现该条数据相对应的“编辑计费方式”弹窗。若直接修改计费方式选项，则需红字提示“与之前的计费方式不一致，请确认后继续操作”，此时点击“修改”按钮，出现的是新计费方式对应的计费详细弹窗。

- 失效：在编辑时，“失效”按钮置灰。

- 启用：在编辑时，“启用”按钮置灰。

**保存校验规则**

同一个供应商、同一个服务类型、同一个成本类型、同一时间、生效状态下只支持维护一条价格维护数据。当编辑后的价格维护数据与之前的数据发生有效期重叠时，不允许编辑，并做出相应的提示。

当截止日期早于当前日期时，不允许保存。

<a id="doc-21A5C4A8A4-section-05bc00aca127"></a>
#### 1.1.5 启用供应商价格规则

供应商价格维护列表界面，勾选一条价格维护数据，点击“启用”按钮，弹出“供应商价格规则启用”弹框。只有“失效”状态的供应商价格规则才能使用启用功能。

**操作规则**

- 启用：完成指定价格数据的状态转换，返回“供应商价格维护列表”界面并刷新。

- 状态流转：启用后，价格规则状态由“失效”变为“已生效”。

- 取消：取消启用，返回“供应商价格维护列表”界面。

**启用校验规则**

同一个供应商、同一个服务类型、同一个成本类型、同一时间、生效状态下只支持维护一条价格维护数据。当生效后的价格维护数据与之前的数据发生有效期重叠时，不允许启用，并做出相应的提示。

<a id="doc-21A5C4A8A4-section-3b13a22cc310"></a>
#### 1.1.6 失效供应商价格规则

供应商价格规则界面，点击“失效”按钮，弹出“供应商价格规则失效”弹框。只有“生效”状态的价格规则数据才进行失效处理。

**操作规则**

- 失效：完成对指定价格规则的失效处理，返回“供应商价格规则列表”界面并刷新。

- 状态流转：失效后，价格规则状态由“已生效”变为“失效”；超过截止时间时，价格规则状态也变为“失效”。

- 取消：取消失效，返回“供应商价格规则列表”界面。

<a id="doc-21A5C4A8A4-section-91ad11ed4121"></a>
#### 1.1.7 梯度价格设置页面

供应商价格维护-新增/编辑页面，勾选梯度报价后，点击“梯度报价”链接，弹出“区间价格设置”弹窗。

**操作规则**

- 新增：新增一行梯度价格。

- 提交：保存并提交当前编辑的梯度价格数据。

- 删除：删除该行的梯度价格数据。

- 关闭：关闭弹窗，返回供应商价格维护列表页面。

- 编辑：当计费方式选择为首加续时，含税单价不必填，并显示“编辑”按钮。点击“编辑”按钮，出现“首加续”弹窗。

**字段定义**

**界面原型概述 · 梯度价格**

- **区域字段或业务元素**：起始值、结束值、计费方式、计费单位、单价、最低价格、说明。

| 字段或控件特例 | 特例约束或业务结果 |
| --- | --- |
| 起始值、结束值、说明 | 只读。 |
| 计费方式 | 候选项选择；计费方式有以下几种：常规方式、航晟运输报价、梯度报价、首加续报价；当计费方式为“首加续”时，含税单价不必填，显示编辑按钮；必填；无预设值；可编辑。 |
| 计费单位 | 候选项选择；计费单位有以下几种：分单数(个)，主单数(个)，提单计费重(kg)，提单毛重(kg)，按提单件数(件)，提单件数/提单毛重；必填；无预设值；可编辑。 |
| 单价 | 保留小数点后2位；必填；无预设值；可编辑。 |
| 最低价格 | 保留小数点后2位；选填；无预设值；可编辑。 |

<a id="doc-21A5C4A8A4-section-4cd337c02599"></a>
#### 1.1.8 航晟运输报价页面

**操作规则**

- 新增：新增一行运输计费价格。

- 提交：保存并提交当前编辑的运输计费价格数据。

- 删除：删除该行的运输计费价格数据。

- 关闭：关闭弹窗，返回上一级供应商价格维护列表页面。

**字段定义**

**界面原型概述 · 航晟运输报价**

- **区域字段或业务元素**：提货点、卸货点、车型、监管车、特种车、尾板车、单价、最低价格。

| 字段或控件特例 | 特例约束或业务结果 |
| --- | --- |
| 提货点 | 填写省市两级，最后填写详细地点；只读。 |
| 卸货点 | 填写省市区三级，最后填写详细地点；只读。 |
| 车型 | 选项：1.5T；4.2车；6.8车；7.6车；3T；5T；8T；10T；12T；20尺柜；40尺柜；40/45尺柜；只读。 |
| 监管车、尾板车 | 选项：是、否；只读。 |
| 特种车 | 只读。 |
| 单价 | 必填；无预设值；可编辑。 |
| 最低价格 | 选填；无预设值；可编辑。 |

<a id="doc-21A5C4A8A4-section-39a1994c0229"></a>
#### 1.1.9 首加续报价页面

**操作规则**

- 新增：新增一行首加续报价。

- 提交：保存并提交当前编辑的运输计费价格数据。

- 删除：删除该行的首加续价格数据。

- 关闭：关闭弹窗，返回上一级供应商价格维护列表页面。

**字段定义**

**界面原型概述 · 首加续报价**

- **区域字段或业务元素**：起始值、续值、计费单位、含税单价、说明。
- **区域级通用规则**：除所列特例外，字段均只读。

| 字段或控件特例 | 特例约束或业务结果 |
| --- | --- |
| 计费单位 | 取自上一层填写的计费单位。 |
| 含税单价 | 保留小数点后2位；可编辑；必填；无预设值。 |

<a id="doc-21A5C4A8A4-section-692b97bb0fdd"></a>
#### 1.1.10 供应商其他价格维护规则

- 订舱费计算规则：空运成本取订舱管理中主订单订舱完成后的航程信息；提单计费重取提单管理的提单编辑页面。订舱费=空运成本*提单计费重。

- 提单杂费计算规则：提单杂费为数据字典所列各项提单杂费之和。

字段定义见 [数据字典 · 提单杂费代码映射](../PRD.高捷物流系统.第6册.运营支撑/PRD.高捷物流系统.第061篇.数据字典.7A3D9E1F50.md#doc-7A3D9E1F50-mappings-mapping-air-waybill-surcharge-code)。

<a id="doc-21A5C4A8A4-section-4757fe4cc688"></a>
### 1.2 客户价格维护管理

<a id="doc-21A5C4A8A4-section-d38d885b9b09"></a>
#### 1.2.1 应收空运费计算规则

先主后分场景下，应收客户空运费计算规则为运费卖价*提单计费重。其中运费卖价取自主订单管理的货物信息-运费卖价。

先分后主场景下，应收客户空运费计算规则为子订单的运费卖价*子单提单计费重之和。其中运费卖价取自子订单管理—订单录入-子订单创建的运费卖价。

<a id="doc-21A5C4A8A4-section-3d0d8373c547"></a>
#### 1.2.2 其他应收计算规则

取应付作为应收；应收修改的角色授权见[其他应收修改权限](../PRD.高捷物流系统.第6册.运营支撑/PRD.高捷物流系统.第062篇.角色与访问权限.5E31456F30.md#doc-5E31456F30-foundation-actions)。
